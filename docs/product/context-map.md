# NOXUND — Context Map

**Authority class:** DESCRIPTIVE-CURRENT · **Lifecycle:** CURRENT · **Mutability:** EDITABLE
**This is not a decision record; it creates no authority.** It is routing and indexing infrastructure: it reports where things live, what kind of thing each identifier names, and which *explicit* relations connect them. Ambiguity about classification, precedence or supersession is governed by [`DEC-0035`](decisions/DEC-0035-canonical-context-v2-authority-lifecycle-supersession-model.md).
**Freshness.** Verified against canonical main `fae24ba7551a5d9469e7b1d62aa175a1ebf7a90c`, 2026-08-20 (B5). This **replaces** the earlier `13d3777…` base and B4's additive two-base clause: B5 re-read every Phase-B-dependent claim here and established that **no other file this map describes changed** between `13d3777…` and this base, so the remaining rows carry forward on that evidence rather than on an unexamined assumption. **The SHA records the base actually verified; the Phase-B unit state below records what the B5 merge itself makes true** — that merge advances `main` past this SHA and so recreates the ordinary `STALE-UNTIL-REVERIFIED` condition on landing, which is inherent and **not a defect**. Current state is owned by [`current-state.md`](current-state.md) — this map routes to it and does not restate it or its freshness contract.

**Scoped reverification — `262c71907f24375196d13dc6901f64aa354acfad`, 2026-08-20, `DEC-0036` unit.** That base is the descendant merge of the `fae24ba7…` base above (PR #85, landing B5). This unit reverified and repaired **only the claims its own adjudication touches** — the `scope-guardrails.md` row in §1, the `scope-guardrails.md` bullet below §1, the `OD-01`…`OD-07` row in §2, and the `DEC-0036` edge added to §3. **Every other row still rests on the `fae24ba7…` verification and nothing wider should be inferred:** this was a bounded classification unit, not a reverification sweep. The `STALE ≠ WRONG` reading above applies unchanged.

**Scoped reverification — `351a73f99c7f8b103faa0a993ef9094e77f74305`, 2026-08-20, B6 (Phase-B closeout).** That base is the descendant merge of the `262c719…` base above (PR #86). B6 established that the only files changed between the `fae24ba7…` base and this one are the three Phase-B routing artifacts, `DEC-0036` and `scope-guardrails.md`, so **every other row here still rests on the `fae24ba7…` verification**. B6 repaired **only the Phase-B terminal state** — the `Phase A…Phase F` and `B0…B6` rows in §2, the Phase-B destination row in §1, and the `PHASE-B-CLOSEOUT-R1` edge in §3 — and made no wider sweep. `STALE ≠ WRONG` applies unchanged.

**Scoped reverification — `aff75d51ac40348fae041fe068953dc3dd288d38`, 2026-08-21, `C1` (execution topology).** That base is the descendant merge of the `351a73f9…` base above (PR #87). C1 repaired **only what its own decision touches** — one new §1 routing row plus a pointer on the existing `docs/agents/**` row, the `Phase A…Phase F` lifecycle cell in §2, **one new `C0 … C5` identifier-family row in §2**, created at this base to register the four-way `C*` namespace collision, and the two `DEC-0037` edges in §3. **Every pre-existing row this unit did not touch still rests on the `fae24ba7…` verification** and nothing wider should be inferred: this was a bounded governance-topology unit, not a reverification sweep. `STALE ≠ WRONG` applies unchanged.

**Scoped reverification — `45667387f74f92cf9aca532f786f9c8bd519d788`, 2026-08-21, `C2` (`packages/orchestrator` disposition).** That base is the descendant merge of the `aff75d51…` base above (PR #88, landing [`DEC-0037`](decisions/DEC-0037-execution-topology-role-independence-governance-review-function.md)). C1 was docs-only, so the `packages/orchestrator` tree at this base is byte-identical to the one C0 examined (`7457f3f2…`). C2 repaired **only what its own decision touches** — the `Phase A … Phase F` lifecycle cell and the `C0 … C5` identifier-family row in §2, and the two `DEC-0038` edges in §3. **Every pre-existing row this unit did not touch still rests on the `fae24ba7…` verification** and nothing wider should be inferred: this was a bounded disposition unit, not a reverification sweep. `STALE ≠ WRONG` applies unchanged.

**Parallel-state correction — `45667387f74f92cf9aca532f786f9c8bd519d788`, 2026-08-21, `C2` R2.** Canonical `main` is **unchanged** at that base: neither PR #89 (`C2`) nor PR #90 (`C3`) has merged. **Undated cells state post-merge truth; this dated paragraph carries the in-flight picture** — so §2 describes `C2` as **landed**, as it described `C1` pre-merge, and the two are not in conflict (`current-state.md` freshness contract). `C3` was separately and explicitly authorized and **executed in parallel with `C2`**, falsifying this map's `C3`…`C5` NOT-STARTED cells — a **`STALE-DESCRIPTIVE DEFECT`** under `DEC-0035` §4 P4. This round repaired **only** the `Phase A … Phase F` lifecycle cell and the `C0 … C5` identifier-family row in §2, and made **no wider sweep**; every other row still rests on the `fae24ba7…` verification. **No §3 relation edge was added for `C3`**: that table records *explicit landed* edges, and no record PR #90 proposes is on `main` — an edge will be owed only once one lands. `STALE ≠ WRONG` applies unchanged.

**Scoped reverification — two bases, 2026-08-21, `C3` (capability and principal model).** **The `C2` R2 paragraph above is a dated in-flight observation and is now historical:** PR #89 has merged and this revision lands PR #90. Its condition on the §3 table — *"an edge will be owed only once one lands"* — **is satisfied here**, not overridden: `DEC-0039` lands with this revision, so its three edges are owed and are added below. `C3`'s **initial evidence base** was `45667387f74f92cf9aca532f786f9c8bd519d788` (PR #88, landing `DEC-0037`); its **post-`C2` reconciliation base** is `1e7e124a2374917c95bffbb56f5f515d95132ee2`, the descendant merge of `45667387…` (PR #89, landing [`DEC-0038`](decisions/DEC-0038-packages-orchestrator-disposition-reduce-to-approval-integrity-primitives.md)). **Both are recorded rather than collapsed** — `DEC-0039` §3's evidence was measured at the initial base and the facts that could have moved were re-derived at the reconciliation base. C3 repaired **only what its own decision touches** — one new §1 routing row plus a pointer on the existing `docs/agents/**` row, the `C*` lifecycle cells in §2, the two `DEC-0039` edges in §3 and the `DEC-0039 → DEC-0038` edge this reconciliation creates. **Every pre-existing row this unit did not touch still rests on the `fae24ba7…` verification** and nothing wider should be inferred: this was a bounded capability/principal unit, not a reverification sweep. **`C2` landed first; `DEC-0038`'s rows and its REDUCE disposition are preserved unchanged.** `STALE ≠ WRONG` applies unchanged.

**Scoped reverification — `cc0483dda0c9cb4a7dd593e81c7800ff6c6be3ae`, 2026-08-21, `C4` (result and disposition contract).** That base is the descendant merge of the `1e7e124a…` base named in the `C3` paragraph above (PR #90, landing [`DEC-0039`](decisions/DEC-0039-capability-technical-principal-independence-model.md)), and it is the **first base at which both `C2` and `C3` are landed**. C4 repaired **only what its own decision touches** — one new §1 routing row plus a pointer on the existing `docs/agents/**` row, the `Phase A … Phase F` lifecycle cell and the `C0 … C5` identifier-family row in §2, and the three `DEC-0040` edges in §3. It also gave the `product-orchestrator-agent.md` reference inside the `C0 … C5` row a **named section anchor** beside its line locator, because `DEC-0040` D15 makes a bare-number reference malformed for load-bearing use — **the claim itself is unchanged**. **Every pre-existing row this unit did not touch still rests on the `fae24ba7…` verification** and nothing wider should be inferred: this was a bounded result-semantics unit, not a reverification sweep. **`C2` and `C3` landed first; their rows, `DEC-0038`'s REDUCE disposition and `DEC-0039`'s P3 — HYBRID decision are preserved unchanged.** `STALE ≠ WRONG` applies unchanged. **Later round, same unit and same base.** The Product Lead supplied the adjudication that **two** distinct `C3` units each carry a historical `RED` — the second **unlanded, with no file on `main`** — and the venue decision routing the four `UNRESOLVED — LOCATOR ONLY` citations to `C5` Limb A then Limb B. Both are **Product-Lead authority carried by `DEC-0040`, not repository facts**; the §2 `C0 … C5` row is repaired for both, and **no other row was revisited**. `C5` remains **NOT STARTED / NOT AUTHORIZED**.

**Scoped reverification — `495f68e822d2234d8a506a9751b46b03f9ebd490`, 2026-08-21, `C6` (Phase-C closeout).** That base is the descendant merge of the `f88e5ca8…` base (PR #92, landing [`DEC-0041`](decisions/DEC-0041-mechanical-governance-enforcement.md)), and it is the **first base at which every `C1`…`C5` record is landed**. C6 repaired **only what its own GO and merge touch** — one new §1 routing row for the Phase-C terminal state, the `Phase A … Phase F` lifecycle cell and the Phase-C identifier-family row in §2, and one §3 discoverability row for the closeout's routed residuals. **`C6` creates no decision record and therefore owes no `DEC-0035` §10 forward-supersession edge**; the `DEC-0037`…`DEC-0041` edges were each created by their own unit and are **preserved unchanged**. **One `STALE-DESCRIPTIVE DEFECT` (`DEC-0035` §4 P4) predating this unit is repaired in the cell C6 must edit anyway:** the `Phase A … Phase F` cell still described `C5` as `NOT STARTED / NOT AUTHORIZED` while the adjacent identifier row already recorded it `LANDED`. **Every pre-existing row this unit did not touch still rests on the `fae24ba7…` verification** and nothing wider should be inferred: this was a bounded closeout unit, not a reverification sweep. `STALE ≠ WRONG` applies unchanged.

**Scoped reverification — `9f12680d329a6107693bdd9f1b077fdfb17dd0dc`, 2026-08-21, `D0` (Phase-D engineering-quality baseline and charter).** That base is the descendant merge of the `495f68e8…` base named in the `C6` paragraph above (PR #93, landing the Phase-C closeout). `D0` repaired **only what its own GO and merge touch** — two new §1 routing rows, the `Phase A … Phase F` lifecycle cell and one new `D0` unit-family row in §2, and one §3 relation row for the edge its own decision record creates. **It also corrects one §4 guard bullet that its own landing falsifies for D and leaves true for E and F** — a `STALE-DESCRIPTIVE DEFECT` under `DEC-0035` §4 P4, repaired rather than left standing. **Every pre-existing row this unit did not touch still rests on the `fae24ba7…` verification** and nothing wider should be inferred: this was a bounded engineering-quality assessment, not a reverification sweep. **Phase C's rows and the `DEC-0037`…`DEC-0041` edges are preserved unchanged**, and nothing Phase C decided is revisited. `STALE ≠ WRONG` applies unchanged. **Later round, same unit and same base — `D0` R2, on independent-review `HOLD`. The preceding sentence no longer holds in full and is corrected here rather than left to mislead:** one **`STALE-DESCRIPTIVE DEFECT`** (`DEC-0035` §4 P4) predating this unit was found in the Phase-C identifier-family row in §2, whose closing clause asserted in the present tense that Phase D is a `NEXT CANDIDATE` only — falsified by the Product Lead's own separate and explicit Phase-D GO, and contradicting three statements this same unit wrote at this base — the `Phase A … Phase F` row and the `D0` row, both in §2 and both within two rows of it, and the §4 guard bullet. **Exactly one clause of that one row was repaired; no other Phase-C row, cell or §3 edge was touched, and the `DEC-0037`…`DEC-0041` edges remain preserved unchanged.** The repair states what closing Phase C did and **routes** the reader elsewhere for Phase D's current standing rather than restating it, so **no Phase-C decision, disposition, adjudication or relation is revisited, narrowed or rewritten**. R2 also repaired accuracy defects confined to [`DEC-0042`](decisions/DEC-0042-engineering-quality-phase-charter.md) and [`PHASE-D-D0-ENGINEERING-QUALITY-BASELINE-R1`](../result/PHASE-D-D0-ENGINEERING-QUALITY-BASELINE-R1.md); **no claim in this map rests on any of them.** This remained a bounded unit — no wider sweep was made, and no other Phase-C row was modernized.

**Scoped reverification — `3110acff2225e5a5e8e2c5a5ccfebefaca4ec9ae`, 2026-08-22, `D1` (JS/TS toolchain coherence and reproducible install).** That base is the descendant merge of the `9f12680d…` base named in the `D0` paragraph above (PR #94, landing [`DEC-0042`](decisions/DEC-0042-engineering-quality-phase-charter.md) and the Phase-D baseline). `D1` repaired **only what its own GO and merge touch** — one new §1 routing row, the `Phase A … Phase F` lifecycle cell and the `D0 …` unit-family row in §2. **No §3 relation edge is owed:** that table records explicit landed edges between *decision records*, and **`D1` creates none** — it operates entirely under `DEC-0042` and lands no `DEC-00NN` of its own. **Every pre-existing row this unit did not touch still rests on the `fae24ba7…` verification** and nothing wider should be inferred: this was a bounded toolchain-coherence unit, not a reverification sweep. **Phase C's rows and the `DEC-0037`…`DEC-0042` edges are preserved unchanged**, and nothing Phase C or `D0` decided is revisited. `STALE ≠ WRONG` applies unchanged.

**Principle — current truth cheap, history reachable.** Routing is not authorization. Naming a unit here places it; it never authorizes resuming it and settles no open technical question.

**What this answers.** What is this identifier · what type of thing is it · which program or track owns it · is it current, historical, proposed or preserved · is it authority, current description, evidence or orientation · where is its canonical or preserved source · does another artifact supersede, narrow, extend, redirect to or depend on it · does its namespace collide · where do I go next.

**What this is not.** Not a roadmap, not a historical narrative, not a replacement for `current-state.md`, not a per-file catalogue of the markdown artifacts under `docs/**`.

---

## 1. Context index — topic → destination

| I need… | Go to | Class (per `DEC-0035` §9) |
|---|---|---|
| What is true right now; the two axes | [`docs/product/current-state.md`](current-state.md) | DESCRIPTIVE-CURRENT — reverified against this map's base by B5 |
| What binds me; precedence; supersession semantics | [`DEC-0035`](decisions/DEC-0035-canonical-context-v2-authority-lifecycle-supersession-model.md) | INTERNAL-NORMATIVE |
| Who may author, review, accept and merge; minimum topology per risk; what reviewer independence means | [`DEC-0037`](decisions/DEC-0037-execution-topology-role-independence-governance-review-function.md) | INTERNAL-NORMATIVE |
| What `packages/orchestrator` **is** after Phase C — the **REDUCE** disposition, which surfaces are non-authoritative legacy and which are retained as candidate primitives (retention is not adoption) | [`DEC-0038`](decisions/DEC-0038-packages-orchestrator-disposition-reduce-to-approval-integrity-primitives.md) | INTERNAL-NORMATIVE |
| What an actor can **technically** do versus what it is **authorized** to do; which technical principals exist; where independence is process-only rather than mechanical; the principal-separation decision **P3 — HYBRID**; and the answer to the approver-provenance question `DEC-0038` §13 routed here — `NOT CURRENTLY SEPARABLE FROM SINGLE PRINCIPAL` | [`DEC-0039`](decisions/DEC-0039-capability-technical-principal-independence-model.md) | INTERNAL-NORMATIVE |
| What a unit or a role must **return**, and what each part of it means — the six non-collapsing axes (execution state · evidence class · unit disposition · artifact verdict · finding lifecycle · `NEXT`); `UNIT DISPOSITION ≠ ARTIFACT VERDICT`; when a technically accepted artifact may survive a historical unit `RED`; mutation accounting beyond the repository; the standing of the `AgentResult` envelope | [`DEC-0040`](decisions/DEC-0040-governed-result-disposition-closeout-contract.md); the fill-in form is [`docs/agents/handoff-template.md`](../agents/handoff-template.md) | INTERNAL-NORMATIVE — the template is DESCRIPTIVE-CURRENT and grants nothing |
| How to assemble minimum sufficient context for one bounded task | [`docs/product/task-context-pack.md`](task-context-pack.md) | DESCRIPTIVE-CURRENT — format aid; grants no execution authority |
| Locked product scope, MVP definition, thresholds | `context/00_Product_Lead_Decision_Log.md`, `context/01_MVP_Scope_PRD.md` | mixed at clause level |
| The `/context` package, file by file | [`docs/product/context-index.md`](context-index.md) | DESCRIPTIVE-CURRENT |
| All decision records | `docs/product/decisions/**` | INTERNAL-NORMATIVE by default |
| Improvement Phase A terminal state; deferred-work routing | [`PHASE-A-CLOSEOUT-R1`](../result/PHASE-A-CLOSEOUT-R1.md) | DESCRIPTIVE-CURRENT |
| Improvement Phase B terminal state; Phase-B routed residuals | [`PHASE-B-CLOSEOUT-R1`](../result/PHASE-B-CLOSEOUT-R1.md) | DESCRIPTIVE-CURRENT |
| Improvement Phase C terminal state; the `DEC-0037`…`DEC-0041` authority chain read as one ledger; what Phase C accepted as a limitation rather than solved; the agent-capability-gap classification; Phase-C routed residuals; and the exact meaning of `PHASE C COMPLETE` | [`PHASE-C-CLOSEOUT-R1`](../result/PHASE-C-CLOSEOUT-R1.md) | DESCRIPTIVE-CURRENT — a closeout reports a terminal state and **creates no authority**; the binding records are the five listed above |
| What improvement **Phase D** is for and what bounds it — the ten quality domains as durable categories, the active-surface-first rule, the evidence a threshold must carry before it may bind, why `TESTS EXIST` ≠ `TESTS COVER CURRENT ACTIVE BEHAVIOR` ≠ `TESTS RUN IN CI`, the quality-gate honesty rule, the boundaries against Phase C, Phase E, Phase F and Axis 1, and the proposed Phase-D closeout condition | [`DEC-0042`](decisions/DEC-0042-engineering-quality-phase-charter.md) | INTERNAL-NORMATIVE — a charter. It **sets no threshold, adopts no tool and authorizes no unit**; every `D` unit needs its own explicit Product-Lead GO |
| What is actually true of NOXUND's engineering quality right now — surface standing, the Q1–Q10 domain matrix, the CI coverage matrix and the inverse question *can this surface change with no signal firing*, the test inventory and what each suite exercises, dependency and reproducibility findings, maintainability findings, known unknowns, and the proposed `D`-unit roadmap | [`PHASE-D-D0-ENGINEERING-QUALITY-BASELINE-R1`](../result/PHASE-D-D0-ENGINEERING-QUALITY-BASELINE-R1.md) | DESCRIPTIVE-CURRENT — a baseline measurement record, **not** a phase closeout and **not** a decision record; its run outputs carry EVIDENCE at clause level. All transient counts live here, never in `DEC-0042` |
| **What the active NOXUND JS/TS workspace is, and how to materialize its dependencies reproducibly** — the three active members as pnpm itself evaluates them, the one install command, why the legacy package is not a workspace member, the workspace-disposition options rejected and on what evidence, the Node and pnpm policy and why neither was changed, the lockfile before/after identity, and the install probes at the base and at the candidate | [`PHASE-D-D1-JS-TS-TOOLCHAIN-COHERENCE-R1`](../result/PHASE-D-D1-JS-TS-TOOLCHAIN-COHERENCE-R1.md) | DESCRIPTIVE-CURRENT — a toolchain-coherence record for a **mutating** unit, **not** a phase closeout and **not** a decision record; **it creates no authority and `D1` landed no `DEC-00NN`**. Its executed install probes carry EVIDENCE at clause level. **The operative configuration is `pnpm-workspace.yaml` itself**, which states the active membership and the exclusion in-file; this record is the evidence behind it |
| Improvement program A–F; Phase-B unit state | `current-state.md` §B, §G | DESCRIPTIVE-CURRENT |
| Data model, migration ladder, RLS design | `docs/database/mvp-data-model.md`, `migration-plan.md`, `entity-relationship-notes.md`, `rls-review-notes.md` | EVIDENCE; Supabase-era clauses SUPERSEDED |
| Per-phase DB apply/design evidence | `docs/database/HANDOFF-phase*`, `DATA-AI-REVIEW-*`, `DATA-AI-000*` | EVIDENCE |
| Pipeline specs, rubric constants, determinism | `docs/data/DATA-*` | EVIDENCE; ratified clauses bind only via their ratifying DEC (`DEC-0035` §9.1) |
| Channel-data collection track | `docs/data/DATA-COLLECT-002-*`, `docs/infra/RUNBOOK-channel-data-collection.md`, `docs/data/SG-6-*` | EVIDENCE |
| Video-collection track | `docs/data/HANDOFF-data-collect-001-video-track.md`, `docs/data/SG-V*` | EVIDENCE |
| Security reviews and findings | `docs/security/SEC-0001` … `SEC-0027` | EVIDENCE |
| Backend consumption / authz contracts | `docs/backend/BE-0001`, `BE-0002` | EVIDENCE |
| Environments, pipelines, hermetic verify | `docs/infra/INFRA-000*`, `docs/infra/HANDOFF-*` | EVIDENCE |
| Agent contracts, boundaries, review matrix, conflict rules | `docs/agents/**` (start at `agent-registry.md`) | mixed — see `DEC-0035` §9. **`agent-review-matrix.md` item #12 is narrowed by [`DEC-0037`](decisions/DEC-0037-execution-topology-role-independence-governance-review-function.md)** (§3); no other item of that file is changed. Items **#10** and **#13** are **read, not narrowed**, by [`DEC-0039`](decisions/DEC-0039-capability-technical-principal-independence-model.md) §6, which also classifies every remaining identity-naming clause **LIVE** or **LATENT**; that record edits no file in this folder. **[`DEC-0040`](decisions/DEC-0040-governed-result-disposition-closeout-contract.md) reconciles exactly two files here** — `product-orchestrator-agent.md` at **seven** named points (the §*Operating Protocol* result reference, the trust-model heading, the stage-gate pointer, the evidence-class axis split, the §*Governance breach* `RED` line, the §*Operational Invariants Summary* `RED` line, and one new §*Output Format* subsection carrying the `OrchestratorDecision` / role-result / closeout boundary) and `handoff-template.md`, which now carries the result contract while **retaining its scope-impact gate and cross-review triggers as conditional sections**; **no other file in this folder is touched**, and the binding limbs, prohibition lists and the whole `OrchestratorDecision` / `TaskCommand` format contract are untouched |
| Legacy Supabase structural record | `docs/database/SUPABASE-LEGACY-STRUCTURAL-BRIEF-R0.md`, `supabase/README.md` | EVIDENCE / HISTORICAL |
| PostgreSQL successor material | `infra/postgres/**`, `db/legacy-map.md` | **classification OPEN** — `DEC-0035` excludes `packages/**`, `apps/**`, `services/**`, `supabase/**` and `.github/**` from its model and names neither of these; this map does not extend that list |
| Workflow registration and arming state | `.github/workflows/**`; read with `current-state.md` §D | DESCRIPTIVE-CURRENT for the state claim |
| Monorepo layout and stack placement | `docs/foundation/monorepo-structure.md` | DESCRIPTIVE-CURRENT |
| Scope admission; `OD-01`…`OD-07` | `docs/product/scope-guardrails.md` | **DESCRIPTIVE-CURRENT · CURRENT · EDITABLE** — adjudicated by [`DEC-0036`](decisions/DEC-0036-scope-guardrails-authority-class-descriptive-current.md), which discharges the `DEC-0035` §9 deferral. **Carries no INTERNAL-NORMATIVE authority of its own**: it is a compilation, so cite the governing source, not the compilation (see below) |
| Backlog and operating narrative | `docs/product/mvp-backlog.md`, `product-operating-system.md` | DESCRIPTIVE-CURRENT, stale (`DEC-0035` Annex A rows 6–7) |
| Off-main preserved evidence | §5 below | EVIDENCE / HISTORICAL |
| Identifier meaning; namespace collisions | §2 below | — |
| Explicit relations between artifacts | §3 below | — |

**Retired note (formerly †) on `current-state.md`.** This note previously stated that the only commits intervening on that document were B2's own and that no fact in it was known-wrong. **Both were accurate when B3 authored them**, and both were overtaken by B3's and B4's own merges, which intervened, touched this map, and left genuinely stale Phase-B claims behind in that document. B5 reverified `current-state.md` at this map's base, repaired those claims and reset its declared base, so its row above carries no staleness qualifier. **`STALE ≠ WRONG` still holds:** the condition recurs for both documents on the next merge past this base, and it is freshness metadata, never a content defect.

**Boundary with `context-index.md`.** [`docs/product/context-index.md`](context-index.md) remains the older, `/context`-scoped index: twelve per-file entries, in Portuguese, self-declared *"vivo"*, and a restatement of the `/context` source-of-truth hierarchy. **It is untouched by this map.** It stays the per-file reader for `context/**`; this map is repository-wide topic routing plus identifier and relation infrastructure. Where the two overlap, `DEC-0035` §9 governs classification.

**Two adjudications `DEC-0035` routed here. B3 declined both, and neither is adjudicated on this surface** — deciding either here would create classification authority this artifact disclaims in its header. One never needed a decision record; the other now has one, and this map reports where each stands.

- **The `/context` blanket-versus-table ambiguity (§9 reading note) — NO RESIDUAL ROUTING CONSEQUENCE.** `DEC-0035` §9 already states its own reading (*"This record reads the named table as the operative grant"*), and its `context/**` exceptions column separately classifies the **two** `/context` files the §*Source of Truth* table does not name: `README.md` — *class:* **DESCRIPTIVE-CURRENT**; `Head of Product.md` — *class:* **NON-AUTHORITATIVE** (empty file). `context/` holds **12** files and that table names **10**, so **every `/context` file already carries a landed classification** and the blanket-versus-table split changes none of them. This records `DEC-0035`'s landed reading and nothing more: it classifies nothing, amends nothing, and closes no adjudication.
- **The class of `docs/product/scope-guardrails.md` (Annex A row 12) — ADJUDICATED: `DESCRIPTIVE-CURRENT · CURRENT · EDITABLE`, no INTERNAL-NORMATIVE authority of its own.** `DEC-0035` §9 withheld the `docs/product/*.md` family default from that one file and stated its class *"requires explicit B3 adjudication"*; the class did **not** follow mechanically from landed authority, B3 and B5 both declined, and the question was escalated. **The Product Lead adjudicated it and [`DEC-0036`](decisions/DEC-0036-scope-guardrails-authority-class-descriptive-current.md) landed the result** — discharging that deferral **by satisfaction**, so `DEC-0035` is neither superseded nor edited and stays CURRENT (§3 relation table). Three consequences a reader routes by. **(a) The pointer incorporates nothing** — correcting the reading this bullet previously carried: `docs/agents/product-orchestrator-agent.md`:167 is a **routing/locative** reference, not incorporation by reference; its self-contained rule is the *preceding* sentence (*"Tudo que não está nesta lista está fora do MVP até decisão registrada"*), operating by complement over the PO agent's **own** MVP Scope list, and `agent-registry.md`:7 is likewise a bare pointer (`DEC-0036` D2). **(b) *Derived scope-admission weight* creates no authority class** — `DERIVED PRACTICAL / ROUTING WEIGHT ≠ DERIVED NORMATIVE AUTHORITY`; nothing becomes authoritative by being compiled here (`DEC-0036` D3). **(c) Authority stays at the governing source**, and a compilation-versus-source divergence is a `STALE-DESCRIPTIVE` reconciliation question, **never** an authority conflict (`DEC-0036` D4). The two disciplines that made this non-obvious both survive: imperative wording confers no authority (`DEC-0035` §3.1), and §9.1 is **not** generalized into a rule that restated clauses become independently normative (`DEC-0036` D5).

---

## 2. Identifier registry

One row per identifier **family**, not per file. Structural types: **PROGRAM · TRACK · PHASE · GATE · UNIT · DECISION · INVESTIGATION**, plus one type repository reality requires — **RECORD-SERIES**, a document-numbering or document-naming series that names artifacts rather than process steps (specs, handoffs). *Class default* is the family default; a landed per-file declaration always overrides it (`DEC-0035` §8 route 3).

| Family / pattern | Type | Owning program or track | Class default | Typical lifecycle | Where defined / found | Disambiguation |
|---|---|---|---|---|---|---|
| `DEC-NNNN` | DECISION | cross-program | INTERNAL-NORMATIVE | CURRENT unless explicitly superseded | `docs/product/decisions/**` | `DEC-0001` is DRAFT/PROPOSED; `DEC-0003` mixed at clause level; `DEC-0028`/`DEC-0033` byte-frozen + PARTIALLY-SUPERSEDED; `DEC-0005`/`DEC-0021` SUPERSEDED (all per `DEC-0035` §9). **`DEC-0027` has no file on `main`** — historical, preserved only (§5). |
| `DEC-NNNN-ADDENDUM-*` | DECISION | as its base record | INTERNAL-NORMATIVE, additive | CURRENT | `docs/product/decisions/` | A normative addendum to its base record, **not** an independent decision (`DEC-0035` §9). Two exist: `DEC-0024-ADDENDUM`, `DEC-0028-ADDENDUM`. |
| `SEC-NNNN` | INVESTIGATION | cross-program | EVIDENCE | CURRENT as record | `docs/security/` | `SEC-0001`…`SEC-0027`. **Two files share `SEC-0023`.** Imperative wording binds nothing (`DEC-0035` §3.1); the `SEC-0001` §0 Fase-9 veto binds by the grant in `docs/agents/agent-conflict-resolution.md`. `SEC-0027` is the canonical seat of the Fase-2 verify-parity correction. |
| `BE-000N` · `INFRA-000N` · `DATA-AI-000N` | INVESTIGATION | backend · infra · data/AI review tracks | EVIDENCE | CURRENT as record | `docs/backend/`, `docs/infra/`, `docs/database/` | Three independently numbered role-scoped review series — not one sequence, and not continuous with `SEC-NNNN`. |
| `DATA-<TOPIC>-00N` | RECORD-SERIES | data engine | EVIDENCE | mixed at clause level | `docs/data/` | Topics: `AUDIT`, `CHANNEL`, `COLLECT`, `CONST`, `ENTITY`, `OPP`, `REPRO`, `SCORING`, `SG8`. The `-00N` suffix is per topic, not global. **`DATA-SG8-001` resolves on `main` to the design contract**; a historical file claimed the same identity and is DO-NOT-LAND (`DEC-0032` §7). |
| `HANDOFF-*` | RECORD-SERIES | cross-track | EVIDENCE | CURRENT as record; operationally HISTORICAL | `docs/database/`, `docs/infra/`, `docs/data/`, `docs/backend/`, `docs/agents/handoffs/` | Named, not numbered. `docs/agents/handoff-template.md` is a format aid, not a handoff (`DEC-0035` §9). |
| `Fase 1` … `Fase 9` | PHASE | database / migration program (Axis 1) | — (label) | mixed; see source | `docs/database/migration-plan.md` | **Portuguese `Fase N` = database migration phases only.** Migration `0007` (Fase 6) is PARKED; Fase 9 (RLS) sits under the standing `SEC-0001` §0 veto. Applied-state facts: `current-state.md` §C. Not the A–F program; not video-track `Fase A–D`. |
| `Fase A` … `Fase D` | PHASE | video-collection track (`DATA-COLLECT-001`) (Axis 1) | — (label) | plan sequence; not carried to completion | `docs/data/HANDOFF-data-collect-001-video-track.md` §7 | Third distinct use of the word *Fase*. Unrelated to `Fase 1–9` and to improvement `Phase A–F`. |
| `Phase A` … `Phase F` | PROGRAM / PHASE | development-system improvement program (Axis 2) | — (label) | **A COMPLETE · B COMPLETE · C COMPLETE · D STARTED** — Phase C's landed units are `C1`, the execution topology ([`DEC-0037`](decisions/DEC-0037-execution-topology-role-independence-governance-review-function.md)), `C2`, the `packages/orchestrator` disposition — decided **REDUCE** ([`DEC-0038`](decisions/DEC-0038-packages-orchestrator-disposition-reduce-to-approval-integrity-primitives.md)), `C3`, the capability / technical principal / independence model ([`DEC-0039`](decisions/DEC-0039-capability-technical-principal-independence-model.md)), which **executed in parallel with `C2`** under its own separate and explicit Product-Lead GO, `C4`, the governed result / disposition / closeout contract ([`DEC-0040`](decisions/DEC-0040-governed-result-disposition-closeout-contract.md)), `C5`, mechanical governance enforcement ([`DEC-0041`](decisions/DEC-0041-mechanical-governance-enforcement.md)), and `C6`, the terminal closeout, which **creates no decision record** and whose output is [`PHASE-C-CLOSEOUT-R1`](../result/PHASE-C-CLOSEOUT-R1.md); `C0` was unlanded and has no file on `main`. **`PHASE C COMPLETE` is a statement about the improvement program only — never about NOXUND development**. **Phase D — engineering quality — is STARTED**: its first unit `D0` is complete and landed [`DEC-0042`](decisions/DEC-0042-engineering-quality-phase-charter.md), the phase charter, plus [`PHASE-D-D0-ENGINEERING-QUALITY-BASELINE-R1`](../result/PHASE-D-D0-ENGINEERING-QUALITY-BASELINE-R1.md). **`D0` sets no threshold and adopts no tool**. Its second unit `D1` — JS/TS toolchain coherence — is complete; it **creates no decision record**, and its output is [`PHASE-D-D1-JS-TS-TOOLCHAIN-COHERENCE-R1`](../result/PHASE-D-D1-JS-TS-TOOLCHAIN-COHERENCE-R1.md) plus the repaired `pnpm-workspace.yaml`. **`D2` and every later `D` unit are NOT AUTHORIZED and NOT STARTED** **· E–F NOT STARTED** | `current-state.md` §B; A closed by `PHASE-A-CLOSEOUT-R1`, B by [`PHASE-B-CLOSEOUT-R1`](../result/PHASE-B-CLOSEOUT-R1.md), C by [`PHASE-C-CLOSEOUT-R1`](../result/PHASE-C-CLOSEOUT-R1.md); C named by `DEC-0032` §8 and `DEC-0035` §12; D chartered by [`DEC-0042`](decisions/DEC-0042-engineering-quality-phase-charter.md) | **Two questions, two answers.** *Where is the current program model described?* → `current-state.md` §B (DESCRIPTIVE-CURRENT), which records the current Product-Lead A–F model — this is a valid destination for **E and F**, and for **D** it is the state report beside D's own charter. *Is there an independently landed record defining that phase?* → **A–D yes; E and F no.** **A, B and C are defined by closeouts that create no authority, while D is defined by a normative charter — the two are different instruments and the distinction is not cosmetic.** The six-phase sequence as a whole is still defined by no canonical record on `main`, and the labels are not landed authority. |
| `B0` … `B6` | UNIT | improvement Phase B (Axis 2) | — (label) | **B1–B6 landed; Phase B CLOSED** — B4 added [`task-context-pack.md`](task-context-pack.md); B5 reconciled this map and `current-state.md`; B6 added [`PHASE-B-CLOSEOUT-R1`](../result/PHASE-B-CLOSEOUT-R1.md) and the terminal-state repair. B0 was an unlanded in-session assessment with no file on `main` | `current-state.md` §G; `DEC-0035` names B0, B1, B3, B5 | **Collides with the Phase-A blocker labels `B1`/`B2`/`B3`** of `PHASE-A-CLOSEOUT-R1` §1 (Git drift · orphaned branch · active-voice docs) — different namespaces, flagged by `current-state.md` §G. B2, B4 and B6 are defined by no canonical record on `main`. |
| `C0` … `C6` | UNIT | improvement Phase C (Axis 2) | — (label) | **`C0` unlanded** — no file on `main`; its historical disposition is `RED`, adjudicated `CLOSED — REMEDIATED`, and it is never routed from as clean evidence ([`DEC-0037`](decisions/DEC-0037-execution-topology-role-independence-governance-review-function.md) header) · **`C1` LANDED** — the execution topology (`DEC-0037`) · **`C2` LANDED** — the `packages/orchestrator` disposition, decided **REDUCE** ([`DEC-0038`](decisions/DEC-0038-packages-orchestrator-disposition-reduce-to-approval-integrity-primitives.md)) · **`C3` LANDED** — the capability / technical principal / independence model ([`DEC-0039`](decisions/DEC-0039-capability-technical-principal-independence-model.md)), **executed in parallel with `C2`**; **two distinct `C3` units** each carry a historical `RED` disposition with governance finding `CLOSED — REMEDIATED` — the original `PHASE-C-C3-CAPABILITY-AND-PRINCIPAL-MODEL-R1` (out-of-repository `MEMORY.md` write) and the **unlanded, no-file-on-`main`** `PHASE-C-C3-PR90-POST-C2-RECONCILIATION-R2` (convenience writes outside `write_scope`: four scratchpad files, four `/tmp` files, one `/tmp` directory), the second being **Product-Lead adjudication carried by [`DEC-0040`](decisions/DEC-0040-governed-result-disposition-closeout-contract.md) §6 rather than repository fact**, named for provenance only (`DEC-0033` §8) — while each **artifact** holds a review `PASS` — a separation the Product Lead accepted explicitly ([`product-orchestrator-agent.md`](../agents/product-orchestrator-agent.md) §*Stage-Gate Discipline*, the historical-finding and no-rewrite clauses — `:600–:601` at this base, a **locator only** under `DEC-0040` D15), so `DEC-0039` is landed authority and the historical `RED` is **not** rewritten · **`C4` LANDED** — the governed result, disposition and closeout contract ([`DEC-0040`](decisions/DEC-0040-governed-result-disposition-closeout-contract.md)): six non-collapsing axes, one `ROLE RESULT` and one 14-item `GOVERNED UNIT CLOSEOUT`, mutation accounting beyond the repository, and `AgentResult` classified `NON-AUTHORITATIVE LEGACY`; the first `C*` unit adjudicated against both `C2` and `C3` landed · **`C5` LANDED** — mechanical enforcement ([`DEC-0041`](decisions/DEC-0041-mechanical-governance-enforcement.md)), executed under its own explicit Product-Lead GO and carrying **two ordered limbs**, both performed. **Limb A** — additive-forward-note repair of the `UNRESOLVED — LOCATOR ONLY` citations at the citing end (`DEC-0035` §3.3; `DEC-0040` D15, §7), by notes appended to `DEC-0036` and `DEC-0037`; **it is not mechanical enforcement**, it ran **first**, and re-derivation found the `DEC-0037` header debt to be **three** bare locators rather than the one the routing named — all three repaired, **`DEC-0040` not edited and no adjudication revised** (`DEC-0041` §7). **Limb B** — one deterministic prospective reference-durability check (`DEC-0041` §8), classified **`AUTOMATIC DETECTIVE · NOT MECHANICALLY MERGE-BLOCKING`**: `Protect main` carries **no `required_status_checks` rule**, so no CI check can block a merge on this repository, and `C5` was forbidden to change that. **Of the seven adjudicated candidates exactly one is implemented**; five are `NOT MECHANICALLY BINDABLE WITH CURRENT SUBSTRATE` and one is `DEFER / REJECT` (`DEC-0041` §6) — **the result-contract, required-field, status-axis, mutation-accounting, scratch/temp and `F4b` ruleset-drift controls all remain unbuilt, and mutation accounting and write-scope discipline stay `PROCESS ONLY`** (`DEC-0041` D11). **No `AgentResult` V2, no machine-readable result schema, no ruleset, `CODEOWNERS`, reviewer threshold, secret or principal was created.** Landing `C5` authorizes nothing further · **`C6` LANDED** — the terminal Phase-C closeout, executed under its own explicit Product-Lead GO. It **creates no decision record**, and its output is [`PHASE-C-CLOSEOUT-R1`](../result/PHASE-C-CLOSEOUT-R1.md) plus terminal-state maintenance to `current-state.md` and this map. It re-derived the `C0`…`C5` chain from landed files and live read-only repository state — **not** from any prior summary — and found **no authority conflict, no material Phase-C gap, and no remaining reference defect that prevents a fresh operator from determining current authority**. Every remaining named-agent requirement is either satisfiable today or **latent with a landed fail-closed route** (`HOLD` + `AGENT-CAPABILITY-GAP`). **Closing Phase C authorizes nothing** — no successor phase, unit, implementation or adoption derives any authority from it, and `NEXT CANDIDATE ≠ GO` (`DEC-0040` D8), which remains true and is not what was defective here. **This cell records what `C6` did; it is not the register of Phase D's standing** — for that, route to the `Phase A … Phase F` row above and to `current-state.md` §B, never to this cell. *(The clause this replaces read "Phase D is a `NEXT CANDIDATE` only". That was true at `C6`'s base and is false in the present tense: the Product Lead's own separate and explicit Phase-D GO falsified it, and it contradicted the `Phase A … Phase F` row, the `D0` row and the §4 guard bullet at this same base. A **`STALE-DESCRIPTIVE DEFECT`** under `DEC-0035` §4 P4, on the same footing as the two repaired in `current-state.md`, repaired here rather than left standing — **never an authority defect**, and nothing `C6` decided is revisited.)* | `DEC-0037` §9; `DEC-0038` §8; `DEC-0039` §7, §8; `DEC-0040` §9; `DEC-0041` D2, §6, §9; [`PHASE-C-CLOSEOUT-R1`](../result/PHASE-C-CLOSEOUT-R1.md); `current-state.md` §B | **Four-way collision, and this is the newest `C*` namespace — the only one owned by Axis 2.** The four prior landed uses are all Axis 1 and all unrelated: the **SG-6 human-gate chain** (`C1` GO · `C2` dispatch · `C3` collect approval · `C4` verify approval · `C5` failure handling) at `docs/data/SG-6-data-collect-002-channel-collection-closeout.md`:24–:32; **`SEC-0021` §7 arming conditions `C1–C5`** at `docs/security/SEC-0021-youtube-collection-env-audit.md`:22 and :109; the **video-track Fase-C task labels** `C1 · DevOps`, `C2 · Security`, `C3 · DevOps` in `docs/data/HANDOFF-data-collect-001-video-track.md` §7 *Sequência segura de implementação* (locator `:99–:101`); and **`C5 = doc-only`** in `docs/database/HANDOFF-phase6-design.md` §3 *Diff de schema*, the *"Untracked por design"* callout (locator `:22`). **Not one series.** Distinct also from improvement `Phase C` itself, which is the phase these units belong to, not a member of this family. |
| `D0` … | UNIT | improvement Phase D — engineering quality (Axis 2) | — (label) | **`D0` LANDED** — the engineering-quality baseline and phase charter, executed under the Product Lead's own explicit Phase-D and `D0` GO. Its outputs are [`DEC-0042`](decisions/DEC-0042-engineering-quality-phase-charter.md), the charter, and [`PHASE-D-D0-ENGINEERING-QUALITY-BASELINE-R1`](../result/PHASE-D-D0-ENGINEERING-QUALITY-BASELINE-R1.md), the evidence record. **`D0` is `ASSESS · CLASSIFY · DEFINE · ROUTE`** — it sets **no threshold**, adopts **no tool**, and changes no manifest, lockfile, compiler configuration, workflow, ruleset or repository setting. It proposes a `D1`…`D5` roadmap and nominates a `NEXT CANDIDATE`; **`NEXT CANDIDATE ≠ GO`** (`DEC-0040` D8), and each unit requires its own explicit Product-Lead GO (`DEC-0042` §D16) · **`D1` LANDED** — JS/TS toolchain coherence and reproducible install, executed under its own explicit `D1` GO. **It creates no decision record**, operating under [`DEC-0042`](decisions/DEC-0042-engineering-quality-phase-charter.md); its outputs are [`PHASE-D-D1-JS-TS-TOOLCHAIN-COHERENCE-R1`](../result/PHASE-D-D1-JS-TS-TOOLCHAIN-COHERENCE-R1.md) and **one repaired file, `pnpm-workspace.yaml`**. It established **by execution** that `pnpm install --frozen-lockfile` **failed** at its base — four evaluated workspace projects against three lockfile importers, `ERR_PNPM_OUTDATED_LOCKFILE` — closing `D0` §12 item 1, which had recorded that as `UNPROVEN — NOT PROBED`. **The active JS/TS workspace is now root, `apps/web` and `packages/shared`**, matching the lockfile exactly; the install succeeds from a clean checkout with **`pnpm-lock.yaml` byte-identical and zero dependency versions changed**. **No CI, no test framework, no root script, no Node selector and no upgrade were added**, and **no file under `packages/orchestrator/**` changed** — tree object identical at `7457f3f2…`. **Excluding the legacy package from the workspace is not removing it** (`DEC-0038` D3), and `D1` neither revisited **REDUCE** nor adopted any of its code. **A later explicit Product-Lead scope extension added one further repair:** `apps/web/tsconfig.json` now extends `tsconfig.base.json` — the `D0` §10.2 row 1 defect and the **sole material toolchain incoherence** its independent reviewer identified. The remaining `UNPROVEN` install inputs stay **routed and not solved** at that record's §15. **The `D1` UNIT carries a permanent historical `RED — VERIFIED AUTHORIZATION BREACH`** from a **reviewer-side** breach, with findings `G-1` and `G-1b` **`CLOSED — REMEDIATED`** — closure **never rewrites a historical `RED`** (`DEC-0040` D7) — while the **technical artifact verdict is `PASS`** under a Product-Lead-accepted unit/artifact separation (`DEC-0040` D14). **`UNIT DISPOSITION ≠ ARTIFACT VERDICT`** (`DEC-0040` D6), and ratification is the Product-Lead merge. **This cell routes rather than restates: the canonical provenance surface is that record's §19 and §20.** **`D2` and every later `D` unit are NOT AUTHORIZED and NOT STARTED**, and **`D1` being `D2`'s declared prerequisite places `D2` without permitting it** · **`D2`…`D5` are not landed labels** — they are placements proposed by the baseline record, never standing identifiers (`DEC-0033` §8) | `current-state.md` §B; [`DEC-0042`](decisions/DEC-0042-engineering-quality-phase-charter.md); [`PHASE-D-D0-ENGINEERING-QUALITY-BASELINE-R1`](../result/PHASE-D-D0-ENGINEERING-QUALITY-BASELINE-R1.md) | **A new `D*` namespace, entering a space that is already crowded — and the crowding is stated rather than counted.** Established here: the Phase-A Unit-D containment chain `D-S1` · `D-S2` · `D-X1a` · `D-X1b` · `D-X2` (its own row below), **Axis 2 but a different program**; the **within-record decision-item series** `D1`, `D2`, … that [`DEC-0037`](decisions/DEC-0037-execution-topology-role-independence-governance-review-function.md) through [`DEC-0042`](decisions/DEC-0042-engineering-quality-phase-charter.md) each number internally, which are item anchors inside one document and not unit labels at all; and at least three further independent `D-N` / `D-A` finding series in `docs/data/` and `docs/database/`, plus the unrelated `SEC-D03` control label. **No exhaustive count is claimed, and none should be inferred** — establishing one is an indexing task this unit did not perform. **The operative rule: a bare `D1` is ambiguous and must always be qualified** — *Phase-D unit `D1`* versus *`DEC-00NN` D1*. Distinct also from improvement `Phase D` itself, which is the phase these units belong to, not a member of this family. |
| `U1` · `U3` · `PHASE-A-EXIT-CHECK-R1` · `PHASE-A-REMAINING-WORK-CLOSEOUT-ASSESSMENT-R1` | UNIT | improvement Phase A (Axis 2) | — (label) | closed | `PHASE-A-CLOSEOUT-R1` header | **Collides with the SG-8 work-program `U*` labels** in the next row. |
| `U1` · `U2` · `U3A-DECOUPLE` · `U3A-GRANTS` | UNIT | SG-8 reproducibility work program (Axis 1) | — (label) | see source records | `DEC-0023`, `DEC-0025`, `DEC-0026`, `DATA-SG8-001`, `docs/agents/handoffs/HANDOFF-U3A-GRANTS.md` | Data-engine units. Not the Phase-A `U1`/`U3`. |
| `SG-0` … `SG-8` | GATE | channel-data collection track (`DATA-COLLECT-002`) (Axis 1) | — (label) | SG-1 and SG-2 green at registration; **SG-0 recorded *herdado* (inherited), not green**; SG-6 closeout landed; others per source | checklist sourced from `SEC-0019`, registered in `DEC-0018` | **`SG-8` carries two senses — see §4.** Per-gate status lives in the track's own records and is not restated here. **No `SG-9` exists.** |
| `SG-V0` … `SG-V8` | GATE | video-collection track (`DATA-COLLECT-001`) (Axis 1) | — (label) | SG-V1/V3/V6/V7 records landed; others per source | `docs/data/HANDOFF-data-collect-001-video-track.md` §7 | **Deliberately separate namespace from `SG-N`** — the source states the `SG-V*` label exists *"para não colidir com a numeração do 002"*. Explicit non-conflation. |
| `P5-REPRO-01` | GATE | data-engine reproducibility (Axis 1) | — (label) | recorded as blocking before first publish | `docs/data/DATA-REPRO-001-p5-repro-harness-spec.md`; registered as the `SG-8` slot in `DEC-0018` | A single gate identifier, not a ladder. **Not** PG-EXIT `P5`, **not** `Fase 5`. |
| `P0` … `P10` (PG-EXIT) | PHASE | PG-EXIT program (Axis 1) | — (label) | **Every item P2–P10 is DEFINED** by the `DEC-0028` §9 plan table (*"proposal — nothing executed; each gated docs/design→GO"*). Per item: **P0/P1** closed by `DEC-0028` · **P2 DEFINED and IMPLEMENTED — landed local/dev only** · **P3** series adjudicated (next row) · **P4·P5·P6·P7·P8 DEFINED, nature *design/impl on GO*, no execution evidence on `main`** · **P9** not started · **P10** has not occurred | plan table at `DEC-0028` §9 (P2–P10); P0/P1 in the `DEC-0028` header | **Defined-but-not-executed is not UNKNOWN.** Per-item qualifiers below the table. Collides by shape with `P5-REPRO-01` and with `Fase N`. |
| `P3A` · `P3B` · `P3B2` · `P3C` | UNIT | PG-EXIT P3 (migration-runner proof) (Axis 1) | — (label) | P3A → Sqitch rejected (`DEC-0029`); P3B/P3B2 → Flyway rejected (`DEC-0030`); **P3C = preserved evidence only** | `DEC-0029`, `DEC-0030`, `DEC-0032`; P3C material in §5 | Runner selection remains open: `DEC-0032` §5 authorizes nothing positively. |
| `S1-PREP-R*` | UNIT | PG-EXIT P3C execute-pre (Axis 1) | — (label) | HISTORICAL / preserved only | preserved ref only (§5) | Eight revisions preserved, `R9` highest, `R6` absent. Never main-landed authority. |
| `D-S1` · `D-S2` · `D-X1a` · `D-X1b` · `D-X2` | UNIT | Phase-A Unit-D containment chain (Axis 2) | — (label) | all closed | `DEC-0033` §4; `D-X1b` preserved evidence routed in §5 | Memorialized as **verified outcomes, not retroactive authorization**; `DEC-0033` §8: nothing re-armed. |
| `OD-01` … `OD-07` | DECISION | product scope | **DESCRIPTIVE-CURRENT** — the register travels with its containing file (`DEC-0036` D6), unless a row has an independent authoritative source establishing something more specific | OD-01/02/05 recorded resolved; OD-03/04/06/07 recorded open | `docs/product/scope-guardrails.md` | **Two-digit series.** A row's normative consequence traces to the record that decided it, never to the row (`DEC-0036` D6). **`OD-02`'s *"Resolvida → Supabase Auth"* is historical** — `DEC-0005` is SUPERSEDED by `DEC-0028`, Supabase is retired, and the successor auth provider is `DEC-0028` §8 `OD-6`, **open**. `OD-06` (where decisions are recorded) remains formally OPEN — `DEC-0035` Annex A row 9. `OD-05` also appears in `docs/foundation/monorepo-structure.md`. |
| `OD-1` … `OD-7` | DECISION | PG-EXIT | INTERNAL-NORMATIVE via `DEC-0028` | OD-1 decided (§4); OD-5 closed by `DEC-0029`; OD-2/3/4/6/7 open | `DEC-0028` §4 and §8 | **One-digit series — not the two-digit one.** `DEC-0032` §6 binds non-conflation for **one pair only** (historical `DEC-0027` §4 `OD-N` vs `DEC-0028` §8 `OD-N`) and names `DEC-0028` §8 as the current namespace where applicable. **It does not bind a general cross-series rule.** |
| `OD-DB-01` … `OD-DB-08` | DECISION | database model | as its adjudicating record | `OD-DB-01/04/06/07` ratified by Data/AI; items conditioned on Security/Backend remain DRAFT/PROPOSED (`DEC-0035` §9) | raised in `docs/database/mvp-data-model.md`; adjudicated by `DEC-0003` | `OD-DB-07` is ratified in `docs/database/DATA-AI-REVIEW-mvp-data-model.md` and cited in `DEC-0003`'s status line, but is **not enumerated in `DEC-0003`'s own decision body**. |
| `OD-A` … `OD-E` | DECISION | Supabase legacy structural inspection | EVIDENCE-borne open questions | see source | `docs/database/SUPABASE-LEGACY-STRUCTURAL-BRIEF-R0.md` | Letter series. Unrelated to every numeric `OD` family. |
| `OD-V1` · `OD-V2` | DECISION | video-collection track | as its adjudicating record | `OD-V2` closed by `DEC-0020`; `OD-V1` recorded OPEN at the source | `docs/data/HANDOFF-data-collect-001-video-track.md`; `DEC-0020` | Video namespace. |
| `OD-PROV-02` | DECISION | Fase-2 provenance gap (Axis 1) | as its ratifying record | opened in `DEC-0009`; **resolved by `DEC-0010`** | `DEC-0010` title and status line; apply evidence in `docs/database/HANDOFF-phase2-apply-closeout.md` | A sixth, standalone `OD` namespace. Route to the ratifying decision record, not to the handoff that evidences it. |
| `checkpoint/*` · `spike/*` · `wip/*` · legacy branch refs | — (Git refs) | preserved evidence | EVIDENCE | HISTORICAL | §5 | **Preservation is not adoption** (`DEC-0035` §9). |

**PG-EXIT per-item qualifiers.** Each is defined by `DEC-0028` §9; none of P4–P8 carries execution evidence, and none is authorized here.

| Item | Weakest accurate status | Qualifier |
|---|---|---|
| **P2** | DEFINED · **IMPLEMENTED AND LANDED on `main`** — local/dev only | `infra/postgres/**`, 14 tracked files, landed via PR #67 merge `46198f5` (commits `86b3f03`, `2a1b052`). **Not production provisioning:** `PHASE-A-CLOSEOUT-R1` §2C records `infra/postgres` as local/dev plus design/spike material, **not a deployed production successor**. |
| **P4** | DEFINED · *design/impl on GO* · no execution evidence | Port legacy `0001` → vanilla: `noxund_identity.users`, `is_admin()` via transaction-local actor, drop Supabase-role grants, Security re-review. |
| **P5** | DEFINED · *design/impl on GO* · no execution evidence | Port legacy `0002`–`0006` → vanilla (mechanical: drop `anon`/`authenticated` grantees); verifies updated. **Not** `Fase 5`, **not** `P5-REPRO-01`. |
| **P6** | DEFINED · *design/impl on GO* · no execution evidence | Rewrite the hermetic CI harness without the Supabase CLI, preserving loopback-only / no-secrets / SHA-pin / always-destroy. **The unit stands; its named mechanism does not** — §9 specifies *"Compose + Sqitch"*, and `DEC-0029` closed `OD-5` as **REJECT SQITCH**. Runner selection remains open (`DEC-0032` §5). |
| **P7** | DEFINED · *design/impl on GO* · no execution evidence | Vanilla design of the SG-8 units (former `0008` ~intact; former `0009` rewritten per `OD-2`); *"Resolves PR #65's destination"*. **Depends on `OD-2`, still open** at `DEC-0028` §8. |
| **P8** | DEFINED · *design/impl on GO* · no execution evidence | Retire obsolete env/secrets/vars; re-point Environments (`production-db`→self-hosted; `youtube-collection` keeps `YOUTUBE_API_KEY`). **Ordering constrained by later authority — see the `P8`→`P9` and `DEC-0034`→`DEC-0033` §5 rows in §3.** `PHASE-A-CLOSEOUT-R1` §4 routes its content (`SUPABASE_DB_URL`→`NOXUND_DB_URL`, workflow re-pointing) to future successor implementation. |

**Nine distinct ladders, not one.** **Axis 1 (product / engineering):** `Fase 1–9` (DB) · `Fase A–D` (video) · `SG-0…SG-8` (channel data) · `SG-V0…SG-V8` (video) · `P5-REPRO-01` (repro gate) · PG-EXIT `P0…P10` · `P3A/P3B/P3B2/P3C`. **Axis 2 (development-system improvement):** Unit-D `D-S*`/`D-X*` · improvement `Phase A–F`. There is **no global lifecycle** and no single progress number.

---

## 3. Supersession and relation table

**Explicit edges only.** A row is `EXPLICIT` when the *from* or *to* artifact states the relation in its own text. **Chronological adjacency is not a relation** (`DEC-0035` §4 P2); anything not stated is absent from this table rather than inferred into it. Where a stated relation does not map cleanly onto one token, the scope column carries the exact limit. `REDIRECTS-TO` rows are presented from the reader's side — *if you are reading X, also read Y for this scope* — and assert **no** supersession; the underlying statement always comes from the *to* artifact.

| From | Relation | To | Scope | Source of the edge | Kind |
|---|---|---|---|---|---|
| `DEC-0007` | REDIRECTS-TO | `DEC-0008` | the consolidated `run_migration` gate board only — recorded closed, Fase 1 applied and verified | landed in-file marker in `DEC-0007` (*"FECHADO (2026-06-24) — ver DEC-0008"*) | EXPLICIT |
| `DEC-0009` | REDIRECTS-TO | `DEC-0010` | resolution of `OD-PROV-02`, opened in `DEC-0009` | `DEC-0010` title and status line (*"Resolve a `OD-PROV-02` aberta na DEC-0009"*) | EXPLICIT |
| `DEC-0017` | PARTIALLY-SUPERSEDES | `DATA-OPP-001` | the `raw_score`-based ranking-key proposal only (item 2, `OPP-03`) | `DEC-0017` item 2 (*"Supersede a proposta `raw_score`-based do `DATA-OPP-001`"*) | EXPLICIT |
| `DEC-0017` | PARTIALLY-SUPERSEDES | `DATA-CHANNEL-001` | the set of four ordered gates only (item 5, `channel-filter-v1`) | `DEC-0017` item 5 (*"Supersede o conjunto de 4 gates ordenados"*) | EXPLICIT |
| `DEC-0004` | EXTENDS | `context/02_Stack_Infra_Architecture.md` §7 | closes a **missing enumeration** in the API surface — the follow-up response-capture route; no scope expansion | `DEC-0004` title and §*Escopo* (*"Fecha enumeração faltante em `02_…` §7"*) | EXPLICIT |
| `DEC-0015` | EXTENDS | `DEC-0014` | records `DEC-0014` closed by the entity-candidates apply, and the named `OPEN-DATA-ENTITY-001` item resolved in the live schema | `DEC-0015` item 4 (*"**DEC-0014 está fechada** por este apply"*) | EXPLICIT |
| `DEC-0019` | EXTENDS | `DEC-0017` | closes the named open item *"self_channel micro-open"* only | `DEC-0019` item 1 (*"**Fecha** o item aberto … da DEC-0017"*) | EXPLICIT |
| `DEC-0022` | PARTIALLY-SUPERSEDES | `DEC-0019` §2 | the classification of the Channel Filter's tolerance as **by-design** (and of the defensive hardening as optional **P2**) — item 3 *"remove e inverte"* that tolerant path, and item 4 names that classification as the *"ponto específico"* superseded. **Ingestion ownership of DC2-01 enforcement is NOT superseded** — item 1 expressly keeps it with the gated collection layer (*"Nada no DEC-0019 §2 quanto a ownership de ingestão é revogado"*). **Amends; expressly does not revoke** — `DEC-0019`'s body is not rewritten, and the decision hierarchy reads §2 *through* `DEC-0022` | `DEC-0022` title and status line (*"**Emenda** o DEC-0019 §2 (não o revoga)"*); `DEC-0022` item 4 | EXPLICIT |
| `DEC-0023` | EXTENDS | `DATA-AUDIT-001` | closes the governance residue `OPEN-A/B/C` only; alters no version, hash or golden digest | `DEC-0023` title and status line (*"Fecha o resíduo de governança do `DATA-AUDIT-001` (OPEN-A/B/C)"*) | EXPLICIT |
| `DEC-0023` §D-F | EXTENDS | `DATA-CHANNEL-001` · `DATA-OPP-001` · `DATA-CONST-001` · `DATA-SCORING-001` | authorizes **additive addenda and SUPERSEDED markers only**, each bound to an already-existing decision; introduces no new decision and never rewrites an original body | `DEC-0023` §D-F | EXPLICIT |
| `DEC-0025` | PARTIALLY-SUPERSEDES | `DATA-SG8-001` | §5.3 and §R **in their LLM-specific parts**; `Q-3`/`Q-5` **in their provider/model parts** | `DEC-0025` header *Supera explicitamente* | EXPLICIT |
| `DEC-0025` | PARTIALLY-SUPERSEDES | `DEC-0024` item 4 | *"Fronteira LLM (Q-3/§5.3) fora do digest"* — item 4 of `DEC-0024`'s **Decisão** list, **not** item 4 of its later hardening list (*"Default-deny reforçado"*) | `DEC-0025` header *Supera explicitamente* | EXPLICIT |
| `DEC-0026` | NARROWS | `DEC-0025` §*Fronteira e não-goals* | unblocks that clause's non-authorization of migration `0009` **exclusively for `U3A-GRANTS`, design-only** — no apply, merge, Environment or secret | `DEC-0026` header *Relaciona* (*"esta decisão a **destrava** especificamente para U3A-GRANTS"*) | EXPLICIT |
| `DEC-0028` | SUPERSEDES | `DEC-0005` | Auth = Supabase, whole record | `DEC-0028` header; §4 closing line | EXPLICIT |
| `DEC-0028` | SUPERSEDES | `DEC-0021` | RO-1 Supabase auto-pause mitigation | `DEC-0028` header | EXPLICIT |
| `DEC-0028` | PARTIALLY-SUPERSEDES | `context/02_Stack_Infra_Architecture.md` | Supabase-managed stack portions only | `DEC-0028` header (**only** — the in-file note at `context/02`:5 does not name `DEC-0028`) | EXPLICIT |
| `DEC-0034` | PARTIALLY-SUPERSEDES | `context/02_Stack_Infra_Architecture.md` | the permanent Supabase retirement; the remainder is preserved as the historical stack record | landed in-file note at `context/02`:5, which names **`DEC-0034` only** | EXPLICIT |
| `DEC-0028` + `DEC-0034` | PARTIALLY-SUPERSEDES | `context/00_Product_Lead_Decision_Log.md` | the Supabase Auth/DB/Cron choice only | landed in-file note in `context/00`; `DEC-0035` §9 | EXPLICIT |
| `DEC-0028` | NARROWS | historical `DEC-0027` | neutralizes the **remote-apply intent** only | `DEC-0028` header | EXPLICIT |
| `DEC-0028-ADDENDUM` | EXTENDS | `DEC-0028` | meaning of the two `DEC-0027` references inside the frozen body; introduces no new decision | addendum header | EXPLICIT |
| `DEC-0024-ADDENDUM` | EXTENDS | `DEC-0024` | migration ordering policy; alters neither the decision nor its SQL artifacts | addendum header | EXPLICIT |
| `DEC-0029` | EXTENDS | `DEC-0028` | §3 and §8; does not edit `DEC-0028` | `DEC-0029` header | EXPLICIT |
| `DEC-0030` | EXTENDS | `DEC-0029` | §7 (successor candidates) | `DEC-0030` header | EXPLICIT |
| `DEC-0030` | EXTENDS | `DEC-0028` | §3 and §8 | `DEC-0030` header | EXPLICIT |
| `DEC-0032` | EXTENDS | `DEC-0028` · `DEC-0029` · `DEC-0030` | `DEC-0028` §3/§8; the two runner rejections | `DEC-0032` header | EXPLICIT |
| `DEC-0032` | SUCCESSOR-OF | historical `DEC-0027` §3 | the **durable content of §3 only**; the §4 `OD-N` labels stay HISTORICAL | `DEC-0032` title, §1, §6, §7 | EXPLICIT |
| `DEC-0033` | EXTENDS | `DEC-0028` | §6, §7 and §9; edits none of `DEC-0028`…`DEC-0032` | `DEC-0033` header | EXPLICIT |
| PG-EXIT `P8` | DEPENDS-ON | PG-EXIT `P9` | binding precondition; **superseded by `DEC-0034` §4 only for the legacy Supabase retirement path**. P8 still requires its own GO (`DEC-0028` §9) | `DEC-0033` §5; `DEC-0034` §4 | EXPLICIT |
| `DEC-0034` | PARTIALLY-SUPERSEDES | `DEC-0028` §10 | the *Security & Privacy* dataset-protection condition, **legacy dataset only**; §9's P9 export/restore limb withdrawn for that dataset | `DEC-0034` header; §4 table | EXPLICIT |
| `DEC-0034` | PARTIALLY-SUPERSEDES | `DEC-0033` §5 (and §6 final ¶ by its own terms) | the P9-before-P8 precondition, **legacy Supabase retirement path only**; `DEC-0033` §8 undisturbed | `DEC-0034` header; §4 table | EXPLICIT |
| `DEC-0035` | EXTENDS | `DEC-0032` §8 · `DEC-0022` §4 · `DEC-0023` §D-F · `DEC-0031` · `DEC-0034` §7 | Canonical Context V2 routing; additive-only doctrine; Phase-A boundary. **Edits no prior DEC** | `DEC-0035` header | EXPLICIT |
| `DEC-0036` | EXTENDS | `DEC-0035` §9 (`docs/product/*.md` row) · `DEC-0035` Annex A row 12 | **Discharges by satisfaction** the classification deferral those clauses reserved for `docs/product/scope-guardrails.md` (*"its class requires explicit B3 adjudication"*), landing **DESCRIPTIVE-CURRENT · CURRENT · EDITABLE** with **no INTERNAL-NORMATIVE authority of its own**. **Not a supersession of any grade:** §9 is neither edited nor narrowed — it withheld the family default and required an adjudication, and this supplies one — so `DEC-0035` stays **CURRENT**. **This row is the external forward-discoverability mechanism `DEC-0035` §10 requires**, `DEC-0035` being FROZEN and `DEC-0036` not authorized to append to it | `DEC-0036` header (*Extends* / *Discharges*), §4, §5 | EXPLICIT |
| [`DEC-0037`](decisions/DEC-0037-execution-topology-role-independence-governance-review-function.md) | EXTENDS | `DEC-0035` §12 · §14 | supplies the **execution-topology limb only** of the Phase-C work §12 reserved, and the permanent topology §14 expressly declined to ratify. **Not a supersession of any grade:** neither clause is edited or narrowed — §12 reserved the work and §14 disclaimed permanence rather than forbidding it — so `DEC-0035` stays **CURRENT**. Prospective only: Phase-A/B practice is **not** retroactively converted into standing authority | `DEC-0037` header (*Extends*), §5, §9 | EXPLICIT |
| [`DEC-0037`](decisions/DEC-0037-execution-topology-role-independence-governance-review-function.md) | NARROWS | `docs/agents/agent-review-matrix.md` item **#12** | one exact point: the mandatory reviewer is the **independent governance-review function**, not the operational existence of `governance_integrity_agent` — which stays `PROPOSED-NOT-OPERATIONAL / RUNTIME-NOT-WIRED` and binds nothing. **The independence requirement is untouched**: no self-review, no silent omission, and a `TASK-SCOPED GOVERNANCE REVIEWER` is never labelled as the registered agent. **Item #11 and the `apply_exact_remediation` chain are LATENT and untouched** (that capability is absent from the runtime allow-list, so #11's trigger cannot fire); **no other item, and no other agent identity, is changed.** The matrix is EDITABLE, so it also carries the additive in-file pointer `DEC-0035` §10 requires | `DEC-0037` header (*Narrows*), D9, §6, §7; the additive note below the matrix in `agent-review-matrix.md` | EXPLICIT |
| [`DEC-0038`](decisions/DEC-0038-packages-orchestrator-disposition-reduce-to-approval-integrity-primitives.md) | EXTENDS | `DEC-0037` §9 | supplies the **`C2` limb only** of the Phase-C work §9 reserved — the `packages/orchestrator` disposition, decided **REDUCE**. **Not a supersession of any grade:** §9 is neither edited nor narrowed — it reserved the question and expressly declared the topology to hold under all three dispositions — so `DEC-0037` stays **CURRENT** and is confirmed rather than disturbed. The one-way authority direction `C1 GOVERNANCE MODEL → RUNTIME IMPLEMENTATION` is preserved | `DEC-0038` header (*Extends*), §8, §15 | EXPLICIT |
| [`DEC-0038`](decisions/DEC-0038-packages-orchestrator-disposition-reduce-to-approval-integrity-primitives.md) | DISCHARGES | `DEC-0032` §8 | **Discharges by satisfaction** the **DEFER-PHASE-C** routing of `packages/orchestrator/**` and *"approver-provenance and consumption-ledger atomicity"*. **The deferral was of a decision, and the decision is now made** for all three limbs: the package is **REDUCE**d to non-authoritative legacy with a named candidate-primitive subset; ledger atomicity is adjudicated **VERIFIED single-process / UNPROVEN multi-process**; **approver provenance is answered by assignment to `C3`, not by solution, and remains technically OPEN**. **Not a supersession of any grade:** §8 is neither edited nor narrowed — it withheld the question and this supplies an answer — so `DEC-0032` stays **CURRENT**. **This row is the external forward-discoverability mechanism `DEC-0035` §10 requires**, `DEC-0032` being FROZEN and `DEC-0038` not authorized to append to it | `DEC-0038` header (*Discharges*), §3, §8, §13 | EXPLICIT |
| [`DEC-0039`](decisions/DEC-0039-capability-technical-principal-independence-model.md) | EXTENDS | `DEC-0037` §9 · `DEC-0037` D12 | supplies the **capability and technical-principal limb** of the `C3` work §9 reserved, including *"the satisfiability of every identity-naming clause other than #12"*, and **re-derives D12's two `REPORTED` GitHub-configuration claims, promoting them to `VERIFIED`** — `required_approving_review_count: 0`, `required_reviewers: []`, `require_code_owner_review: false`, and **`DIRECT PUSH TO main = MECHANICALLY BLOCKED` confirmed, the withdrawn direct-push claim not resurrected**. **Not a supersession of any grade:** §9 reserved the work and D12 marked its own provenance honestly — neither is edited or narrowed — so `DEC-0037` stays **CURRENT** and **FROZEN**. **This row is the external forward-discoverability mechanism `DEC-0035` §10 requires**, `DEC-0037` being FROZEN and `DEC-0039` not authorized to append to it | `DEC-0039` header (*Extends*), §3, §9 | EXPLICIT |
| [`DEC-0039`](decisions/DEC-0039-capability-technical-principal-independence-model.md) | REDIRECTS-TO | `docs/agents/agent-review-matrix.md` items **#10** and **#13** | reader-side only: if you are reading either item, read `DEC-0039` §6 for how it is satisfied. **#10** — *"Documentação que altera decisões → Product Orchestrator"* — names the **decision-registration / gate-confirmation** function `DEC-0037` D10 grants the Product Orchestrator, **not** an independent review function it withholds; reconcilable at clause granularity, **no `HOLD — AUTHORITY-CONFLICT`**, and the approval that makes a decision valid remains the Product Lead's manual merge (`DEC-0037` D11). **#13** — its trigger **does not fire** for a unit that changes no boundary; when it does fire, `security_agent` has no REAL PRODUCT EXECUTOR and that is a `DEC-0035` §4 **P4** divergence to **escalate, never route around**. **Asserts no supersession and narrows nothing** — #12's narrowing remains `DEC-0037`'s alone, is not extended by analogy, and `DEC-0039` edits no file under `docs/agents/**` | `DEC-0039` §6.2, §6.3, §6.4, §9 | EXPLICIT |
| [`DEC-0039`](decisions/DEC-0039-capability-technical-principal-independence-model.md) | DISCHARGES | `DEC-0038` §13 · §15 | **Discharges by satisfaction** the `APPROVER PROVENANCE = OPEN · ROUTED TO C3` assignment those clauses made. **The routed item was a question assigned for adjudication, and it is discharged by being adjudicated — not by being solved**, the same distinction `DEC-0038` §3 draws about its own discharge of `DEC-0032` §8. `DEC-0039` D6 returns `TECHNICAL APPROVER-PROVENANCE = NOT CURRENTLY SEPARABLE FROM SINGLE PRINCIPAL` and D9 adopts **P3 — HYBRID**: **no authenticated provenance is implemented, no trust anchor is provisioned, and no primitive retained by `DEC-0038` D2 is represented as supplying provenance** (`DEC-0038` §13). **Not a supersession of any grade:** §13 and §15 are neither edited nor narrowed — they routed the question and this supplies the answer — so `DEC-0038` stays **CURRENT** and **FROZEN**, and its **REDUCE** disposition is neither rewritten nor reopened. **This row is the external forward-discoverability mechanism `DEC-0035` §10 requires**, `DEC-0038` being FROZEN and `DEC-0039` not authorized to append to it | `DEC-0038` §13, §15; `DEC-0039` header (*Discharges*), §7, §9 | EXPLICIT |
| [`DEC-0040`](decisions/DEC-0040-governed-result-disposition-closeout-contract.md) | EXTENDS | `DEC-0037` §9 | supplies the **`C4` limb only** of the Phase-C work §9 reserved (*"`C4` — `AgentResult` V2 / result schema"*). **Stated plainly rather than by quiet substitution: §9 named that limb by the shape it expected — a schema — and `DEC-0040` supplies it as a *process contract* instead**, because `DEC-0038` D1 reduced the runtime that would have hosted a schema to non-authoritative legacy and `DEC-0037` is itself `RUNTIME-AGNOSTIC`, so a governance contract existing only as TypeScript would bind and reach nothing. **Not a supersession of any grade:** §9 reserved the work and this supplies it — neither §9 nor any of D1–D13 is edited or narrowed — so `DEC-0037` stays **CURRENT** and **FROZEN**, and its D1 taxonomy, D5/D6 topology, D7 independence criteria, D10 orchestrator constraints and D11 GO/merge semantics are consumed unchanged. **This row is the external forward-discoverability mechanism `DEC-0035` §10 requires**, `DEC-0037` being FROZEN and `DEC-0040` not authorized to append to it | `DEC-0040` header (*Extends*), §4, §10 | EXPLICIT |
| [`DEC-0040`](decisions/DEC-0040-governed-result-disposition-closeout-contract.md) | DISCHARGES | `DEC-0038` D2 · §15 | **Discharges by satisfaction** the *"Routed elsewhere, not decided here"* assignment of `src/core/result-schema.ts` and the `AgentResult` envelope to `C4`, and §15's note that *"`C4` inherits an unconstrained field"*. **The routed item was a question assigned for adjudication, and it is discharged by being answered** — the same distinction `DEC-0038` §3 draws about its own discharge of `DEC-0032` §8. `DEC-0040` D16 returns **OPTION B**: the envelope is `NON-AUTHORITATIVE LEGACY`, a **partial transport projection** of a role result that cannot express unit disposition, artifact verdict, evidence class, finding lifecycle, mutation accounting or role/instance/principal separation, and whose required `agent: string` asserts a registered identity `DEC-0037` D2/D3 forbid a task-scoped role from claiming. **Zero code action, carrying `DEC-0038` D3 forward: no file under `packages/orchestrator/**` is created, edited or removed, and no `AgentResult` V2 is written.** **Not a supersession of any grade:** D2 and §15 are neither edited nor narrowed, and `DEC-0038`'s **REDUCE** disposition is neither rewritten nor reopened — so `DEC-0038` stays **CURRENT** and **FROZEN**. **This row is the external forward-discoverability mechanism `DEC-0035` §10 requires** | `DEC-0038` D2, §15; `DEC-0040` header (*Discharges*), D16, §8, §10 | EXPLICIT |
| [`DEC-0040`](decisions/DEC-0040-governed-result-disposition-closeout-contract.md) | DISCHARGES | `DEC-0039` D14 · §8 | **Discharges by satisfaction** the assignment that *"`C4` … is the natural carrier for capability and principal metadata"*, together with §8's list of what a result must be **able to express**. `DEC-0040` D10 consumes it — `ROLE ≠ INSTANCE ≠ PRINCIPAL`, `UNKNOWN` as a required outcome rather than a gap to fill by assumption, the exact label `PROCESS-INDEPENDENT · PRINCIPAL-NOT-INDEPENDENT`, no inference of principal separation from instance separation, and **no field of any result proving Product-Lead identity** — and D11/D12 place it in the role result and the unit closeout. **`DEC-0040` provisions no principal, selects no trust anchor and does not reopen approver provenance**, which stays `DEC-0039` D9's **P3 — HYBRID**. **Not a supersession of any grade:** D14 and §8 are neither edited nor narrowed — D14 expressly left the shape to `C4` — so `DEC-0039` stays **CURRENT** and **FROZEN**. **This row is the external forward-discoverability mechanism `DEC-0035` §10 requires** | `DEC-0039` D14, §8; `DEC-0040` header (*Discharges*), D10–D12, §9, §10 | EXPLICIT |
| [`DEC-0041`](decisions/DEC-0041-mechanical-governance-enforcement.md) | EXTENDS | `DEC-0040` §9 | supplies the **`C5` limb only** of the Phase-C work §9 reserved — the six routed mechanical-enforcement candidates, **adjudicated** rather than built. **`EXTENDS`, not any supersession grade:** §9 routed work to a later authorized unit and `DEC-0041` supplies it; nothing in `DEC-0040` is replaced, narrowed or declared wrong, and §9's own rule that each candidate *"requires `C5`'s own explicit Product-Lead GO"* remains exactly true. **Stated plainly rather than smoothed: §9 listed six candidates and `DEC-0041` implements one** (the `D15` reference check), disposing of the other five as `NOT MECHANICALLY BINDABLE WITH CURRENT SUBSTRATE` or `DEFER / REJECT` — which is an adjudication of the limb, not a partial performance of it. `DEC-0040` stays **CURRENT** and **FROZEN** | `DEC-0040` §9; `DEC-0041` header (*Extends*), D2, §6 | EXPLICIT |
| [`DEC-0041`](decisions/DEC-0041-mechanical-governance-enforcement.md) | DISCHARGES | `DEC-0040` D15 · §7 | **Discharges by satisfaction** the two-limb Product-Lead routing decision §7 recorded — `EXISTING LOCATOR DEBT → C5 ADDITIVE-FORWARD-NOTE REPAIR (LIMB A)` and `FUTURE LOCATOR DRIFT → C5 MECHANICAL CHECK (LIMB B)` — together with D15's closing residual (*"A mechanical staleness check is routed to `C5`"*). **`DISCHARGES`, not `EXTENDS`: §7 stated a requirement to *perform*, and performance spends it** — Limb A is done and Limb B is landed. **What is discharged is the routing, never D15 itself**, which stays fully **CURRENT** and binding: `LINE NUMBER = LOCATOR / CONVENIENCE ONLY`, a drifted locator is a stale locator and **not** an authority defect, and **no general citation framework and no retroactive obligation is created**. `DEC-0040` is **not edited** and stays **FROZEN** | `DEC-0040` D15, §7 (the routing block); `DEC-0041` header (*Discharges*), D3, §7, §8 | EXPLICIT |
| [`DEC-0041`](decisions/DEC-0041-mechanical-governance-enforcement.md) | DISCHARGES | `DEC-0039` D11 · §8 | **Discharges by satisfaction the *venue*, and only the venue:** §8 assigned the mechanical-enforcement candidates to `C5` *"under its own GO"*, and `C5` has adjudicated them. **The `F4b` detection D11 called for is NOT delivered and NOT satisfied** — `DEC-0041` D9 finds it unbuildable on this substrate (it needs an admin-scoped secret `C5` may not create, and, decisively, **its baseline would be owned by the single admin it exists to detect**) and re-routes it out-of-band to Phase F. **`F4b`'s disposition is neither rewritten nor reopened** and remains `ACCEPTED CURRENT LIMITATION` + `DETECTION REQUIRED LATER`; **`CURRENT ENFORCEMENT` and `CONFIGURATION DURABILITY` stay separate questions**, and every protection `DEC-0039` recorded was re-verified in force at `DEC-0041`'s base. `DEC-0039` is **not edited** and stays **CURRENT** and **FROZEN** | `DEC-0039` D11, D15, §8; `DEC-0041` header (*Discharges*), D9, §6 candidate 7 | EXPLICIT |
| [`DEC-0041`](decisions/DEC-0041-mechanical-governance-enforcement.md) | REDIRECTS-TO | [`DEC-0036`](decisions/DEC-0036-scope-guardrails-authority-class-descriptive-current.md) · [`DEC-0037`](decisions/DEC-0037-execution-topology-role-independence-governance-review-function.md) | **reader-side only:** a reader who arrives at either record's line-number citations into `docs/agents/product-orchestrator-agent.md` should read the **`C5` additive forward-status note appended at the end of that same file**, which supplies the durable identity — stable path, stable named section, verbatim quotation — for each repaired locator. **`REDIRECTS-TO`, not `NARROWS` and not any supersession grade:** a locator was repaired at the citing end under `DEC-0035` §3.3, and **nothing in either body is narrowed, weakened or reopened** — `DEC-0036` D1–D6 and `DEC-0037` D1–D16 stand exactly as landed, and both records remain **FROZEN** and **ACTIVE / CURRENT**. **The old numbers stay in the text as `HISTORICAL / STALE LOCATOR`** and are never the identity | `DEC-0041` §7; the `C5` note at the end of each record; `DEC-0040` D15, §7 | EXPLICIT |
| [`DEC-0042`](decisions/DEC-0042-engineering-quality-phase-charter.md) | EXTENDS | `DEC-0041` §10 | supplies the **Phase-D charter** that clause named in terms — *"Documentation-quality standards, general testing policy and code-quality policy are Phase D"*. **The limit is on the record and is not decorative: `DEC-0042` opens that venue and expressly does not supply its content.** No documentation-quality standard, testing policy, code-quality policy, threshold or tool adoption is set — each stays the subject of a later `D` unit under its own explicit Product-Lead GO (`DEC-0042` §D6, §D16). **`EXTENDS`, not any supersession grade:** §10 stated a boundary and reserved work for a later phase, and nothing in `DEC-0041` is replaced, narrowed or declared wrong — its control-strength vocabulary is inherited unchanged and its own GO rule is restated rather than relaxed. `DEC-0041` remains **FROZEN** and **ACTIVE / CURRENT**, and **is not edited**: the edge lives here because `DEC-0035` §10 directs an external table entry for exactly that case | `DEC-0042` header *Extends* and §6 | EXPLICIT |
| `DEC-0009` | REDIRECTS-TO | `DEC-0031` | the numbering-identity question only; `DEC-0031` expressly does not alter `DEC-0009` | `DEC-0031` header *Relaciona* / *Escopo*; `DEC-0035` Annex A row 3 | EXPLICIT |
| `DEC-0010` | REDIRECTS-TO | `DEC-0031` | canonical Fase-2 authority; `DEC-0010` unaltered | `DEC-0031` header | EXPLICIT |
| `SEC-0008` | REDIRECTS-TO | `SEC-0027` | Fase-2 verify-parity correction; `SEC-0008` corrected **additively**, body unchanged | `DEC-0031` header; `DEC-0035` §9 | EXPLICIT |
| `SEC-0009` | REDIRECTS-TO | `DEC-0031` | attribution reinterpreted additively | `DEC-0031` header | EXPLICIT |
| `SG-8` (gate slot) | REDIRECTS-TO | `P5-REPRO-01` | the gate slot's content is the repro harness, blocking before first publish | `DEC-0018` gate-checklist row | EXPLICIT |
| `PHASE-A-CLOSEOUT-R1` §4 | REDIRECTS-TO | improvement Phase B · Phase C · future successor work | deferred sets, routed and not solved | `PHASE-A-CLOSEOUT-R1` §4 | EXPLICIT |
| `PHASE-B-CLOSEOUT-R1` §5 | REDIRECTS-TO | Product-Lead decision · improvement Phase C · future successor work | Phase-B residuals, routed and not solved. **Closeout records are DESCRIPTIVE-CURRENT and supersede nothing** — this row is discoverability, not a `DEC-0035` §10 forward-supersession edge | `PHASE-B-CLOSEOUT-R1` §5 | EXPLICIT |
| [`PHASE-C-CLOSEOUT-R1`](../result/PHASE-C-CLOSEOUT-R1.md) §9 | REDIRECTS-TO | Phase F · a future capability / principal unit · the Product-Lead-authorized documentation-reconciliation unit · Product-Lead decision · the product / engineering axis | Phase-C residuals, routed and **not solved** — write-scope prevention and out-of-band ruleset-drift detection to Phase F; a second technical principal or trust anchor to a future authorized unit; agent-doc reconciliation and the newly reported nonblocking documentation findings to the reconciliation unit `DEC-0038` §14 established; the Phase-B residual set inherited **unabsorbed**. **Closeout records are DESCRIPTIVE-CURRENT and supersede nothing** — this row is discoverability, not a `DEC-0035` §10 forward-supersession edge, and `C6` created no decision record to owe one | `PHASE-C-CLOSEOUT-R1` §9, §12 | EXPLICIT |
| `current-state.md` | DEPENDS-ON | `DEC-0035` | classification, precedence and supersession semantics | `current-state.md` header, §F | EXPLICIT |
| this map | DEPENDS-ON | `current-state.md` · `DEC-0035` | current-state facts; classification model | this document's header | EXPLICIT |

**Extraction scope — a corrected positive claim, not a clean negative.** Edges were extracted by reading **both** header vocabularies this corpus actually uses: the English verbs (`Supersedes`, `Extends`, `Discharges`) **and** the Portuguese relational verbs (`Supera`, `Emenda`, `Revoga`, `Anula`, `Substitui`, `Fecha`, and `destrava` **only where its object is a document clause**).

An earlier draft of this map asserted that `DEC-0001`…`DEC-0026` declare no edges. **That assertion was wrong**, because it rested on an English-only extraction. The `DEC-0004`, `DEC-0007`, `DEC-0009`, `DEC-0015`, `DEC-0017`, `DEC-0019`, `DEC-0022`, `DEC-0023`, `DEC-0025` and `DEC-0026` rows above are the Portuguese-declared edges it missed.

Every row above was read in its source file — but **exhaustive absence across the early corpus is not claimed**. Treat a missing early-`DEC` edge as *not yet extracted*, never as *verified absent*.

**What was deliberately not encoded.** `destrava` ("unblocks") occurs 21 times across `DEC-0003`…`DEC-0031`; in all but one it unblocks a **task or a gate**, not a document, and none of those is a relation. The single relational use is `DEC-0026`'s, encoded above. **No edge is manufactured here**, and `DEC-0035` declares that it edits no prior DEC. Byte-frozen records (`DEC-0028`, `DEC-0033`) cannot carry their own forward edges — this table is their external forward-reference surface (`DEC-0035` §3.3, §10).

---

## 4. Known-false-assumption guard

Compact corrections to assumptions a fresh reader is likely to form.

- **No `SG-9` definition exists.** The `SG-*` ladder ends at `SG-8`: outside this map's own guard text, `docs/` and `context/` carry no definition of, or reference to, an `SG-9`. A reader who meets the token is looking at an assumption, not at a record.
- **`SG-8` names two different things, and the evidence classes differ.** Both are navigable; only the link between them is a reading.
  - **(a) The gate slot** — `SG-8` in the channel-data checklist, content `P5-REPRO-01` pre-publish. **EXPLICIT**, sourced at the `DEC-0018` gate-checklist row (§2).
  - **(b) The body of work carried under the SG-8 name** — `DATA-SG8-001`, `DEC-0024`/`0025`/`0026`, units `U1`/`U2`/`U3A-*`, migrations `0008`/`0009`. **Observable fact**: those artifacts exist and are named `sg8-*`.
  - **(c) The transition between them is INFERRED** — the reading that the label *evolved* from (a) into (b). **No artifact declares any change of meaning**; `DATA-SG8-001` calls it *"o gate SG-8"* / *"gate de reprodutibilidade"* throughout. Treat the drift as **inferred historical usage, not established fact**.
  - **`SG-8` was not terminally completed** in either sense.
- **`P3C` and `S1-PREP-R*` exist only as preserved evidence** (§5). Neither was ever main-landed authority.
- **No production successor PostgreSQL exists**, and **migration-runner selection remains open** — three mechanisms rejected, none adopted (`DEC-0032` §5). See `current-state.md` §C.
- **Improvement Phase E and Phase F are Product-Lead program definitions**, not independently landed phase records. **Phase D no longer belongs in that set:** [`DEC-0042`](decisions/DEC-0042-engineering-quality-phase-charter.md) is a landed defining record for D, so **A–D are landed and E and F are not**. This is **not** "nothing to route to" for E and F: the current A–F program model is described in `current-state.md` §B. What does not exist is an independently landed **E or F** defining record. **Two further corrections a reader needs, so the repair is not over-read.** First, the instruments differ: A, B and C are closed by **closeouts, which create no authority**, whereas D is defined by a **normative charter** — being landed is not the same as being of the same kind. Second, **a landed charter is not a landed phase completion**: Phase D is **STARTED, not complete**, and `DEC-0042` §D16 states that every `D` unit still requires its own explicit Product-Lead GO.
- **Prepared / registered ≠ executed.** A plan-table row, a registered workflow, an authored migration and a named unit are each a preparation, never evidence of execution.
- **Registry-active collection workflow ≠ armed.** `youtube-collection.yml` and `video-collection.yml` are registry-`active` and `workflow_dispatch`-only, yet **disarmed and fail-closed**: `.github/collection/` does not exist on `main`, and marker absence *is* the disarmed state (`current-state.md` §D).
- **A compilation is not an authorization.** `docs/product/scope-guardrails.md` compiles scope rules and is **DESCRIPTIVE-CURRENT, carrying no INTERNAL-NORMATIVE authority of its own** (`DEC-0036` D1). A rule binds by the source that originated it, **never by being copied, summarized or listed there** — so cite and follow the governing source, not the compilation (`DEC-0036` D4). Where the compilation and a declared source enumerate differently, that is a **`STALE-DESCRIPTIVE` reconciliation or an open product-scope question, never an authority conflict**; one such difference is routed and undecided at [`PHASE-B-CLOSEOUT-R1`](../result/PHASE-B-CLOSEOUT-R1.md) §5 row 5.
- **Naming is not authorization.** Every identifier in §2 is indexed so it can be found. Resuming any of them requires an explicit Product-Lead GO (`DEC-0035` §6).

---

## 5. Preserved evidence — routing only

Off-`main` refs retained by design. **Every row is EVIDENCE / HISTORICAL. Preservation is not adoption** (`DEC-0035` §9): a preserved ref is citable by exact SHA and is **never** routed from as current state or current authority. The SHAs below are provenance, not the freshness base.

| Ref | SHA | Files | What it preserves |
|---|---|---|---|
| `checkpoint/pg-exit-p3c-execute-pre-2026-08-11` | `4382c78` | 403 | PG-EXIT P3C execute-pre working state |
| `checkpoint/phase-a-primary-2026-08-11` | `151fb46` | 353 | Pre-reconciliation primary checkout; carries the P3C design / execute-pre doc set |
| `checkpoint/phase2-verify-hotfix-local-2026-08-11` | `42c5952` | 148 | Local Fase-2 verify hotfix state |
| `checkpoint/phase5-computed-metrics-local-2026-08-11` | `7cdad73` | 233 | Local Fase-5 computed-metrics state |
| `feat/sg8-r0-preflight-author-production-db` | `2d0fbcd` | 319 | SG-8 R0 remote-promotion preflight — **holds the historical `DEC-0027` record and the DO-NOT-LAND R0 report-format file** (`DEC-0032` §7) |
| `spike/pg-exit-p3a-sqitch-atomicity` | `8b36bd1` | 344 | Sqitch atomicity falsification (→ `DEC-0029`) |
| `spike/pg-exit-p3b-flyway-atomicity` | `e8c72f1` | 350 | Flyway least-privilege falsification (→ `DEC-0030`) |
| `wip/phase6-producer-events-preservation` | `a1ec483` | 286 | Orphaned Fase-6 producer-events SQL; migration `0007` PARKED |
| `origin/phase2/verify-hotfix` | `a5e68b9` | 147 | Fase-2 verify hotfix branch |
| `origin/phase5/computed-metrics-apply` | `e84fbb6` | 179 | Fase-5 computed-metrics apply branch |
| `AdeptLabsDev/noxund-unitd-x1b-run-30450421392-preservation` — external repository | `be83d45` | 11 | **D-X1b**: the Actions logs of run `30450421392`, preserved outside finite Actions retention. A **deliberately maintained preservation surface, not a transient location**: `PRIVATE` · `ARCHIVED` · not a fork · single root commit `be83d455…`, 0 parents · **owner-reversible**, never "immutable" (`DEC-0033` §4). Being private, **a fresh operator may not have access**; class stays EVIDENCE, and `DEC-0033` §8 bars converting it into an adoption |

**Three preserved classes, per `DEC-0035` §9 — two routable, one not.** Git refs and the external preservation repository are listed above and are reachable by exact identifier. The third class, **the local stash entry, is deliberately not given a routing row**: a stash ordinal such as `stash@{0}` is **machine-local and mutable** — it exists only in the clone that created it and moves or vanishes on any later stash operation, so it is not a durable target a fresh operator can reach. The class is recorded here; no pointer is offered, and creating a preservation ref to fix that is outside this unit.

`DEC-0033` §8 expressly forbids converting the D-X1b repository into an adoption or into any product, runtime, build or CI dependency.

**Sentinel residue on preserved refs.** `DEC-0033` §7A records that 18 of 52 remote branches still contain `.github/collection/*.armed` while canonical `main` is clean, classifies this CLEANUP-CANDIDATE / NONBLOCKING, and notes that several of those branches are checkpoint or evidence surfaces. A preserved ref may therefore carry an arming marker that has no force.

`DEC-0033` §7A states safety is fail-closed **three-deep**: (1) sentinel absence on canonical `main`; (2) the **`SEC-F18` main-ref guard** in both collection workflows, which aborts any dispatch whose ref is not `refs/heads/main` and does **not** depend on the Environment; (3) the **GitHub Environment branch policy** restricted to `main` plus a required Product-Lead reviewer. Layers 2 and 3 are what make a dispatch *from* a marker-carrying ref inert.

**P3C preserved design set.** `infra/postgres/spikes/pg-exit-p3c-execute-pre/docs/` on `checkpoint/phase-a-primary-2026-08-11` — 15 files, of which 8 are `S1-PREP-R*` revisions (`R1`–`R5`, `R7`–`R9`; **`R9` highest, `R6` absent**). Preserved design material only.

**Local-ref note.** The local branches `phase2/verify-hotfix` and `phase5/computed-metrics-apply` point at the corresponding `checkpoint/*` SHAs rather than the `origin/*` SHAs listed above; the `origin/*` values are the remote provenance.

**Not routing targets.** Four archives (`context.zip`, `docs.zip`, `design/schema-color.zip`, `infra/postgres/spikes/pg-exit-p3c-execute-pre.rar`) are **absent from every ref**. They are named here only so their absence is not mistaken for a lookup failure. The governing discipline is landed and descriptive, not issued by this map: `PHASE-A-CLOSEOUT-R1` §2A records that known untracked archive residue was *"removed **without inspection**, by exact authorized path only."*

---

*Governed by [`DEC-0035`](decisions/DEC-0035-canonical-context-v2-authority-lifecycle-supersession-model.md). Current state is owned by [`current-state.md`](current-state.md). The `/context` package index remains [`context-index.md`](context-index.md), unmodified.*

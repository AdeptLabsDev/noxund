# NOXUND — Compact Current State

**Authority class:** DESCRIPTIVE-CURRENT · **Lifecycle:** CURRENT · **Mutability:** EDITABLE
**This is not a decision record; it creates no authority.** It does two things: report current reality and route to authority. Ambiguity about classification, precedence or supersession is governed by `DEC-0035` (§I).

**Freshness contract.** LAST VERIFIED AGAINST CANONICAL MAIN = `fae24ba7551a5d9469e7b1d62aa175a1ebf7a90c`, verification date **2026-08-20** (B5 reverification). If `origin/main` advances past that SHA, this document is **STALE-UNTIL-REVERIFIED** for current-state purposes — which does **not** mean every merge changes its content: reverification may conclude **NO CONTENT CHANGE REQUIRED**, updating only this metadata under an authorized maintenance unit. **The SHA is freshness provenance, not normative authority**, and **`STALE ≠ WRONG`**. B5 re-read §A–§F at that base and repaired only the Phase-B unit state (§B, §G, §H, §I): between B2's `a11bd169…` base and this one, the only files that changed are the three Phase-B artifacts themselves, so no claim here about any other file was disturbed. **The SHA records the base actually verified; the §B / §G unit state records what the B5 merge itself makes true.** That merge advances `main` past this SHA and so recreates the staleness condition on landing — inherent to the contract, **not a defect**.

**Scoped reverification — `351a73f99c7f8b103faa0a993ef9094e77f74305`, 2026-08-20, B6 (Phase-B closeout).** That base is the descendant merge of the `fae24ba7…` base above (PR #86, landing `DEC-0036`). B6 established from the repository that **the only files changed between `fae24ba7…` and this base are the three Phase-B routing artifacts, `DEC-0036` and `scope-guardrails.md`** — so every §A–§F claim here still rests on B5's verification, undisturbed. B6 therefore reverified and repaired **only the Phase-B terminal state** (§B, §G, §H, §I) and made no wider sweep. **This revision states what its own merge makes true**; the Product-Lead manual merge is the ratification gate (§E), and that merge recreates the ordinary `STALE-UNTIL-REVERIFIED` condition — inherent, not a defect.

**Scoped reverification — `aff75d51ac40348fae041fe068953dc3dd288d38`, 2026-08-21, `C1` (execution topology).** That base is the descendant merge of the `351a73f9…` base above (PR #87, landing the Phase-B closeout). C1 reverified and repaired **only what its own decision makes stale** — the Axis-2 ladder and the authority paragraph in §B, and §E in whole — resting on agent-capability and `packages/orchestrator` facts it re-derived from the repository at this base. **Every other claim here still rests on the B5 / B6 verifications and nothing wider should be inferred:** this was a bounded governance-topology unit, not a reverification sweep. **§F is deliberately untouched** — its *"no state-authority divergence was found"* is scoped to those verifications, and the one divergence C1 addressed (`agent-review-matrix.md` #12) is resolved at the authority end by [`DEC-0037`](decisions/DEC-0037-execution-topology-role-independence-governance-review-function.md) D9 rather than left standing. `STALE ≠ WRONG` applies unchanged, and this revision's own merge recreates the condition — inherent, not a defect.

**Scoped reverification — `45667387f74f92cf9aca532f786f9c8bd519d788`, 2026-08-21, `C2` (`packages/orchestrator` disposition).** That base is the descendant merge of the `aff75d51…` base above (PR #88, landing [`DEC-0037`](decisions/DEC-0037-execution-topology-role-independence-governance-review-function.md)). C2 reverified and repaired **only what its own decision makes stale** — the Phase-C unit state in §B, its authority paragraph, and the `packages/orchestrator` claims in §E. Those repairs rest on facts C2 **re-derived by execution at this base**: 36/36 tests green, `tsc --noEmit` clean, ten foundation agents exposing 47 actions — 42 via `planningHandler` and 5 bespoke, **all pure** (no `node:fs`, `child_process`, `exec`, `spawn` or `fetch` anywhere under `src/agents/`), with all four registered *sensitive* actions on `planningHandler` — zero code importers outside the package, zero workflow references, and the four approval-gate properties reproduced. **Every other claim here still rests on the B5 / B6 / C1 verifications and nothing wider should be inferred:** this was a bounded disposition unit, not a reverification sweep. **§F is deliberately untouched** — C2 found no new state-authority divergence. `STALE ≠ WRONG` applies unchanged, and this revision's own merge recreates the condition — inherent, not a defect.

**Scoped reverification — `f88e5ca84bc510adb7f9717e2e20296c8830b2d8`, 2026-08-21, `C5` (mechanical governance enforcement).** The dated paragraphs below are in-flight observations at their own bases and are **not rewritten**; per the freshness contract each records what was true when it was written. `C5`'s canonical base is the descendant merge that landed [`DEC-0040`](decisions/DEC-0040-governed-result-disposition-closeout-contract.md). **What `C5` re-derived directly, and therefore vouches for at this base:** the three active rulesets and the full rule set, parameters and bypass scoping of `Protect main` (id `19697151`) — **confirming there is no `required_status_checks` rule**; the sole collaborator and their role; `default_workflow_permissions` and `can_approve_pull_request_reviews`; the `on:` trigger block of **all eleven** workflow files; the four cited propositions in `docs/agents/product-orchestrator-agent.md` and their enclosing sections; the contents of `docs/result/`; the corpus-wide count of line-locator citation tokens; and the absence of the `C4` R4 process-deviation artifact from the working tree and from every reachable Git object. **`F4a` and the protection facts `C3` measured were re-derived and are unchanged.** **One correction to a claim `C5` worked from is recorded rather than absorbed:** **two** workflows carry a `pull_request` trigger, not one — `data-engine-tests.yml` and `sg8-integration-local.yml` — though both are path-filtered away from every governance surface, so the operative conclusion (nothing ran on a governance PR) held. `C5` repaired **only what its own decision makes stale** — the Axis-2 ladder and authority paragraph in §B, and the §E bullets reporting mechanical enforcement, `F4b` detection routing and the citation-drift state. **Every other claim here still rests on the B5 / B6 / C1 / C2 / C3 / C4 verifications, and nothing wider should be inferred:** this was a bounded enforcement unit, not a reverification sweep. **§C, §D, §F, §G and §H are deliberately untouched.** `STALE ≠ WRONG` applies unchanged, and this revision's own merge recreates the staleness condition — inherent, not a defect.

**Parallel-state correction — `45667387f74f92cf9aca532f786f9c8bd519d788`, 2026-08-21, `C2` R2.** Canonical `main` is **unchanged** at that base: neither PR #89 (`C2`) nor PR #90 (`C3`) has merged. **Read that with the freshness contract above, not against the cells below:** per that contract *this revision states what its own merge makes true*, so the undated §B cells describe `C2` as a **landed** unit of Phase C — exactly as they described `C1` pre-merge — while **this dated paragraph carries the in-flight picture**. The two are not in conflict; that division is the convention. `C3` was authorized separately and explicitly by the Product Lead and **executed in parallel with `C2`**, so this document's `C3`…`C5` NOT-STARTED claims became false while `C2` was in flight — a **`STALE-DESCRIPTIVE DEFECT`** under `DEC-0035` §4 P4, repaired here, **not** an authority defect and **no** reason to revisit anything `C2` decided. This round repaired **only parallel-state claims** — the Phase-C unit state in §B and the trailing authorization-state sentence in its authority paragraph — and made **no wider sweep**; every other claim still rests on the B5 / B6 / C1 / C2 verifications. **`DEC-0038` is byte-identical and was not touched**: its only `C3` references are a routing statement and an authority statement, both of which remain true because `C3`'s GO was its own and derived nothing from `C2`. **Nothing about PR #90's content, quality or outcome is asserted anywhere in this document.** `STALE ≠ WRONG` applies unchanged.

**Scoped reverification — two bases, 2026-08-21, `C3` (capability and principal model).** **The `C2` R2 paragraph above is a dated in-flight observation and is now historical:** PR #89 has since merged and this revision lands PR #90. Per the freshness contract, the dated paragraph records what was true at its base and is not rewritten. `C3`'s **initial evidence base** was `45667387f74f92cf9aca532f786f9c8bd519d788` — the descendant merge of the `aff75d51…` base above (PR #88, landing `DEC-0037`) — and its **post-`C2` reconciliation base** is `1e7e124a2374917c95bffbb56f5f515d95132ee2`, the descendant merge of `45667387…` (PR #89, landing [`DEC-0038`](decisions/DEC-0038-packages-orchestrator-disposition-reduce-to-approval-integrity-primitives.md)). **The two are recorded separately rather than collapsed:** `DEC-0039` §3's evidence was measured at the initial base, and the facts that could have moved were **re-derived at the reconciliation base** and are marked there. C3 reverified and repaired **only what its own decision makes stale** — the Axis-2 ladder and the authority paragraph in §B, and the agent/governance bullets in §E that report the independence and protection state, which C3 re-derived **directly from live repository configuration** and promoted from `REPORTED` to `VERIFIED`. **Every other claim here still rests on the B5 / B6 / C1 / C2 verifications and nothing wider should be inferred:** this was a bounded capability/principal unit, not a reverification sweep. **§D was re-derived incidentally and is confirmed unchanged**; **§F is deliberately untouched**, its scoping unaffected. **`C2` landed first and `DEC-0038` is current authority — nothing it decided is revisited, weakened or contradicted here**, and its **REDUCE** disposition is not rewritten. `STALE ≠ WRONG` applies unchanged, and this revision's own merge recreates the condition — inherent, not a defect.

**Scoped reverification — `cc0483dda0c9cb4a7dd593e81c7800ff6c6be3ae`, 2026-08-21, `C4` (result and disposition contract).** That base is the descendant merge of the `1e7e124a…` base named in the `C3` paragraph above (PR #90, landing [`DEC-0039`](decisions/DEC-0039-capability-technical-principal-independence-model.md)), and it is the **first base at which both `C2` and `C3` are landed** — so `C4` is the first unit adjudicated against both. C4 reverified and repaired **only what its own decision makes stale** — the Axis-2 ladder and the authority paragraph in §B, and the §E bullets that report result, evidence and disposition semantics, which `DEC-0040` now lands as authority. It also repaired the `product-orchestrator-agent.md` reference in §B to carry a **named section anchor** beside its line locator, because `DEC-0040` D15 makes a bare-number reference malformed for load-bearing use — **the claim itself is unchanged**. **Every other claim here still rests on the B5 / B6 / C1 / C2 / C3 verifications and nothing wider should be inferred:** this was a bounded result-semantics unit, not a reverification sweep. **§C and §D were not re-derived and are not restated; §F is deliberately untouched** — C4 found no new state-authority divergence, and the one drifted-citation condition it did find is classified by `DEC-0040` D15 as a **stale locator, not an authority defect**, so nothing is left standing for §F to carry. `STALE ≠ WRONG` applies unchanged, and this revision's own merge recreates the condition — inherent, not a defect. **Later round, same unit and same base — two Product-Lead inputs, recorded not re-derived.** The Product Lead subsequently supplied (a) the adjudication that **two** distinct `C3` units each carry a historical `RED`, the second having **no file on `main`**, and (b) the venue decision routing the four `UNRESOLVED — LOCATOR ONLY` citations to `C5` Limb A then Limb B. Both are **Product-Lead authority carried by `DEC-0040`, not repository facts**, and §B is repaired accordingly. **No further repository claim was re-derived for them**, and `C5` remains **NOT STARTED / NOT AUTHORIZED**.

**Reading convention.** `State:` = factually true now. `Authority:` = what a landed normative record requires. Never merged; a fact is never rewritten to fit a rule.

---

## A. Product identity — locked MVP

**State:** the MVP is the **Hotspot Artists Report**, a closed manual-assisted validation product on real data — **not** a marketplace. Initial vertical **Chicago Drill**; locked keyword `chicago drill type beat`. Locked cardinalities: **two fixed reports**, **10 artists each**, **2 `HOT` each**, **30-day window**, **~500 videos per collection run**.

**Authority:** numbers are deterministic — generative AI never produces Score, Velocity, Signals, Competition, ranking or Example; raw YouTube API data is immutable and never overwritten; computed is reconstructible from raw; deliveries are traceable. Sources: [`docs/agents/README.md`](../agents/README.md) §*Regras gerais* 2–4 (INTERNAL-NORMATIVE, `DEC-0035` §9) and `context/03_Data_AI_Agents_Methodology.md`.

Full scope and thresholds: [`01_MVP_Scope_PRD.md`](../../context/01_MVP_Scope_PRD.md).

---

## B. Two axes — do not conflate them

NOXUND runs on **two distinct axes**. The A–F ladder is only the second, and it is **not** NOXUND's development lifecycle.

### Axis 1 — product / engineering trajectory

**State:** the substantive product-build work. It **predates** the A–F program, is substantial and **incomplete as a whole**. It is **not the current active execution focus** — nothing is, at this base — and it was **not globally completed or superseded** by the improvement program. **Repository evidence does not establish the causal reason for the pivot**; any account of it is Product-Lead orientation, not repository fact. Unsettled and relevant here: **the exact resumption point and the next technical unit**.

**Plan, not present fact and not authorization:** it is to be **re-established from canonical repository truth, re-adjudicated as necessary, and resumed under Product-Lead GO**. The exact next post-improvement unit is **not decided**.

One orientation anchor — not a lineage, not to be reconstructed here: successor-database work is unadopted. The P3C review-round variants `R10-R1` / `R10-R2` are **TECHNICAL-ADOPTION-UNDECIDED**, and they and the P3C `POSTHOC-AUDIT-HOLD-REINSPECTION-REQUIRED` disposition are **defined by no canonical record on `main`**, named to withhold endorsement, never to assert current force (`DEC-0032` §5, §8; `DEC-0033` §8).

Collection-track evidence is landed on `main`; this document neither enumerates nor grades it (§I).

### Axis 2 — development-system improvement program (current model: A–F)

An **overlay** that improves the context, governance, agent, engineering and orchestration system used to develop NOXUND.

- **A — Reality & Repository Hygiene:** **COMPLETE**
- **B — Canonical Context V2:** **COMPLETE** (B0–B6 all complete — §G)
- **C — agent-governance / runtime redesign:** **STARTED, not complete.** Its landed units are **C1**, the execution topology ([`DEC-0037`](decisions/DEC-0037-execution-topology-role-independence-governance-review-function.md)) — **COMPLETE**; **C2**, the `packages/orchestrator` disposition — **COMPLETE**, decided **REDUCE** ([`DEC-0038`](decisions/DEC-0038-packages-orchestrator-disposition-reduce-to-approval-integrity-primitives.md)); and **C3**, the capability / technical principal / independence model ([`DEC-0039`](decisions/DEC-0039-capability-technical-principal-independence-model.md)) — **COMPLETE on the landing of PR #90**, having **executed in parallel with `C2` under its own separate and explicit Product-Lead GO**. ****Two distinct `C3` *units*** each carry a historical `RED — VERIFIED AUTHORIZATION BREACH` disposition, both with governance finding `CLOSED — REMEDIATED`: the original `PHASE-C-C3-CAPABILITY-AND-PRINCIPAL-MODEL-R1`, for an unauthorized out-of-repository `MEMORY.md` write; and the **unlanded** `PHASE-C-C3-PR90-POST-C2-RECONCILIATION-R2`, for unauthorized convenience writes outside its `write_scope` — four session-scratchpad files, four `/tmp` files and one `/tmp` directory, whose pre-recognition cleanup was **not** adjudicated as a separate `C0`-style self-remediation aggravation (neither a second finding nor an exoneration). **The second is Product-Lead adjudication carried by [`DEC-0040`](decisions/DEC-0040-governed-result-disposition-closeout-contract.md) §6, not a fact re-derivable from `main`** — that unit has no file on `main` and is named for provenance only (`DEC-0033` §8). **Two `RED`s are not two artifact failures:** the original unit and the reconciliation unit each returned a `PASS` review verdict on what it produced. The original *artifact*'s independent technical review is `PASS`, and the Product Lead explicitly accepted that unit/artifact separation** ([`product-orchestrator-agent.md`](../agents/product-orchestrator-agent.md) §*Stage-Gate Discipline*, the historical-finding and no-rewrite clauses — `:600–:601` at this base, a **locator only** under `DEC-0040` D15). **That is unit conduct, not artifact rejection — the historical `RED` is preserved and never rewritten to `PASS`, and `DEC-0039` is landed authority.** And **C4**, the governed result, disposition and closeout contract ([`DEC-0040`](decisions/DEC-0040-governed-result-disposition-closeout-contract.md)) — **COMPLETE**, the first unit adjudicated against both `C2` and `C3` landed. And **C5**, mechanical governance enforcement ([`DEC-0041`](decisions/DEC-0041-mechanical-governance-enforcement.md)) — **COMPLETE**, under its own explicit Product-Lead GO. **It implements exactly one control against seven adjudicated candidates, and that is its decision rather than a shortfall** (`DEC-0041` D2): the prospective reference-durability check at `tools/governance/`, classified **`AUTOMATIC DETECTIVE · NOT MECHANICALLY MERGE-BLOCKING`** because `Protect main` carries **no `required_status_checks` rule** and `C5` was forbidden to add one. Its Limb A repaired the `UNRESOLVED — LOCATOR ONLY` citations by additive forward-status notes appended to `DEC-0036` and `DEC-0037` under `DEC-0035` §3.3 — **neither body edited, no historical text rewritten, and `docs/agents/product-orchestrator-agent.md` untouched** — and re-derivation found the `DEC-0037` header debt to be **three** bare locators rather than the one the routing named, all three repaired without editing or revising `DEC-0040` (`DEC-0041` §7). **Five candidates are `NOT MECHANICALLY BINDABLE WITH CURRENT SUBSTRATE` and one is `DEFER / REJECT`** (`DEC-0041` §6). **`C6` and a Phase-C closeout do not exist and are NOT AUTHORIZED.** Its predecessor `C0` was an unlanded in-session assessment with **no file on `main`**, carrying a historical `RED` disposition adjudicated `CLOSED — REMEDIATED` — never routed from as clean evidence (`DEC-0037` header).
- **D · E · F:** **NOT STARTED**

**F is the final currently-defined phase of this improvement program — not the final phase of NOXUND development.**

**Provenance.** A–F is the **current Product-Lead improvement-program model**, not a landed ladder. Repository-landed records establish **A–C**; **D–F are Product-Lead-defined future program phases, not yet independently defined as landed phase records**, and the complete six-phase sequence is defined by no canonical record on `main`. Per `DEC-0033` §8, such identifiers are named to place them, never to confer standing.

**State:** B6 was the terminal Phase-B unit and **Phase B is closed** — its terminal record is [`PHASE-B-CLOSEOUT-R1`](../result/PHASE-B-CLOSEOUT-R1.md). **Phase C has begun and is incomplete.** No Phase-C closeout record exists, and the phase as a whole is defined by no canonical record on `main`.

**Authority:** **each Phase-C unit requires its own explicit Product-Lead GO**; the C1 GO authorized C1, the C2 GO authorized C2, the C3 GO authorized C3, the C4 GO authorized C4 and the C5 GO authorized C5, and each expired with its unit (`DEC-0035` §6). **Landing C1, C2, C3, C4 and C5 authorizes nothing further** — not a `C6`, not a Phase-C closeout, not Phase D, E or F, not any implementation, adoption or deletion of `packages/orchestrator` code (`DEC-0038` D3, §16), not the creation of any second technical principal or trust anchor (`DEC-0039` D9, §11), not any machine-readable result schema (`DEC-0040` D16, §12), not any Axis-1 resumption, and no re-arming (`DEC-0033` §8). **`C5` narrows this in one direction only and widens it in none:** it landed **one** CI check, and **created no ruleset, no required status check, no branch protection, no reviewer threshold, no `CODEOWNERS` file, no secret and no principal** (`DEC-0041` D5, D10, §15). **The check it landed cannot block a merge**, and any later claim that it gates `main` is false unless a separately authorized unit adds a `required_status_checks` rule — which `DEC-0041` neither performs nor recommends. **Closing Phase B likewise authorized nothing.** **State, as recorded by C2 R2 and unchanged here:** `C3` **was** separately and explicitly authorized by the Product Lead and executed in parallel with `C2` — that GO was its own and derived nothing from `C1`, `C2` or this document. **Beyond C4, no further successor unit is authorized on either axis at this base**, and no Axis-1 unit is authorized at all.

---

## C. Database and infrastructure reality

**State:**
- Legacy Supabase is **permanently retired**.
- **No production successor PostgreSQL exists.** **P10 (remote production PostgreSQL provisioning, `DEC-0028` §9) has not occurred.**
- `infra/postgres` is **local/dev plus design and spike material**, not a deployed production successor.
- Migrations **0001–0006** belonged to the retired Supabase schema. **0008/0009 were never applied.**
- **No current code path reaches a real production successor database.**

**Undecided, not decided here:** PostgreSQL version, RDS vs self-managed, collation, role topology, backup/PITR, migration runner, P10 architecture.

---

## D. Collection and write capability

**State:**
- The six DB-apply workflows (`phase1`–`phase5`, `entity`) are registry state **`disabled_manually`**.
- `.github/workflows/youtube-collection.yml` and `.github/workflows/video-collection.yml` are registry state **`active`** and **`workflow_dispatch`-only**, but are **DISARMED and FAIL-CLOSED**: their `guard` jobs abort every dispatch because the arming markers `.github/collection/youtube-collection.armed` and `.github/collection/video-collection.armed` are **ABSENT**. The `.github/collection/` directory does not exist on `main`. **Marker absence is the disarmed state, by design.**
- Repository-level Actions **secrets = 0** and **variables = 0**. At environment level, `youtube-collection` retains exactly one secret, `YOUTUBE_API_KEY`, and `production-db` is empty (`PHASE-A-CLOSEOUT-R1` §2B).

Registry-active is **not** armed, and neither collection workflow is disabled: both are **intentionally disarmed / inert — not dead**.

**Authority:** nothing was re-armed. Restoring any write capability the Unit-D containment removed — including re-enabling any of the six registrations and re-arming either collection sentinel — requires its **own explicit Product-Lead decision**, per [`DEC-0033`](decisions/DEC-0033-unit-d-closeout-p9-before-p8-sequencing.md) §8.

---

## E. Agent and governance reality

**Authority:**
- The **Product Lead is the final authority**; a landed record is ratified by their **manual merge** (`DEC-0035` header; [`DEC-0037`](decisions/DEC-0037-execution-topology-role-independence-governance-review-function.md) D11).
- `NEXT` / `READY` / `RECOMMENDED` is **not execution authorization**; mutation needs an explicit Product-Lead GO.
- An Author **must not** review or accept their own work; the Reviewer is a distinct party — `AUTHOR ≠ REVIEWER` (`DEC-0037` D5, with checkable independence criteria at D7).
- **The execution topology is landed authority, no longer practice.** `DEC-0037` fixes the actor taxonomy, the minimum topology per risk class, and that **the Product Lead manually merges every governed PR** until changed by explicit Product-Lead authority.
- **`agent-review-matrix.md` item #12 is narrowed** by `DEC-0037` D9: the mandatory reviewer is the independent governance-review **function**, satisfiable by a distinct `TASK-SCOPED GOVERNANCE REVIEWER` — never by the Author, never silently omitted, and never labelled as the registered agent. **No other item of that matrix is changed**; #11 is latent and untouched.
- **Capability is separated from authority.** [`DEC-0039`](decisions/DEC-0039-capability-technical-principal-independence-model.md) fixes that `ROLE ≠ EXECUTION INSTANCE ≠ TECHNICAL PRINCIPAL`, that `TECHNICALLY POSSIBLE ≠ AUTHORIZED` and `DECLARED ≠ AVAILABLE`, and that **assigning a task-scoped role is an authorization boundary, not a technical capability sandbox** (D7). Every Product-Lead-only action is `PROCESS / AUTHORITY RESERVED · NOT TECHNICALLY ISOLATED` (D4). Its principal-separation decision is **P3 — HYBRID** (D9): single-principal containment accepted for ordinary governed work, with a second principal a prerequisite only for claiming mechanical independence, for any non-human reliance on an approval's authenticity, and for any future automated actor that could mutate irreversibly without a human in the loop. **`DEC-0039` provisions no principal and edits no file under `docs/agents/**`**; its §6 *reads* matrix items #10 and #13 without narrowing either.
- **What a unit returns, and what each part of it means, is now landed authority.** [`DEC-0040`](decisions/DEC-0040-governed-result-disposition-closeout-contract.md) fixes **six independent axes** that must never be collapsed — execution state · evidence class · unit governance disposition · artifact verdict · governance-finding lifecycle · `NEXT` / authorization (D1) — and prohibits compound tokens such as `PASS-WITH-RED`. **`UNIT DISPOSITION ≠ ARTIFACT VERDICT`** (D6), so a unit may be `RED` while its artifact is `PASS` under the eight conditions of D14, including independent artifact review and explicit Product-Lead acceptance of the separation. **`CLOSED — REMEDIATED` never rewrites a historical `RED`** (D7). `HOLD` and `RED` are removed from the *evidence-class* list they had been listed in and returned to the disposition axis — **nothing is revoked**, only re-assigned (D3). **The authorization-breach invariant is now unconditional and prospective:** `ANY VERIFIED AUTHORIZATION BREACH IN A GOVERNED UNIT => UNIT RED` (D5) — no past disposition is re-examined on its authority. One `ROLE RESULT` (D11) and one 14-item `GOVERNED UNIT CLOSEOUT` (D12) replace bespoke per-unit return contracts, with an explicit omission rule. **Mutation accounting covers every write, not only tracked repository files:** `OUTSIDE REPOSITORY ≠ OUTSIDE write_scope`, a clean `git status` is not a `ZERO MUTATION` proof, and **cleanup is never retroactive authorization** (D13). Load-bearing references carry a named anchor; **line numbers are locators only**, and a drifted locator is not an authority defect (D15). **`DEC-0040` writes no code, provisions no principal, and is enforced by nothing mechanical** — that was `C5`, which has now landed and enforces **almost none of it** (below).
- **Mechanical enforcement is landed, and its honest extent is one control.** [`DEC-0041`](decisions/DEC-0041-mechanical-governance-enforcement.md) fixes the binding vocabulary — **`PREVENTIVE`** (the action cannot proceed) · **`AUTOMATIC DETECTIVE`** (checked automatically, deterministic, but does not prevent) · **`PROCESS ONLY`** (no machine binds it) — and two invariants: **a process checklist is never called mechanical enforcement, and a GitHub Action is never called merge-blocking unless repository rules make its success required** (D1). Of **seven** adjudicated candidates it implements **one**: a prospective reference-durability check at `tools/governance/`, Python 3.11 stdlib-only, deterministic, secret-free, evaluating only lines a PR adds or changes on the decision corpus and the routing surfaces. **It detects citation *shape*, not meaning** — it cannot judge load-bearingness, cannot verify an anchor semantically, and **deliberately does not detect drift**, because a drifted locator carrying a durable anchor is not an authority defect (`DEC-0040` D15). **Five candidates are `NOT MECHANICALLY BINDABLE WITH CURRENT SUBSTRATE` and one is `DEFER / REJECT`**, each on a stated reason (`DEC-0041` §6): the result-contract and required-field validators have **no artifact to validate** — `C1`–`C4` produced no result file, `docs/result/` holds two phase closeouts, and `C4` chose a **prose** contract; the status-axis check would flag only the records that *prohibit* the compound tokens; mutation accounting cannot see session scratchpads, system temp, memory or external systems, and **all three observed convenience-write breaches happened exactly there**; write-scope prevention would need a controlled executor, routed to Phase F; and `F4b` ruleset-drift detection would need an admin-scoped secret `C5` may not create and, decisively, **a baseline owned by the very admin it exists to detect**. **No `AgentResult` V2 and no machine-readable result schema was created** (`DEC-0041` D6). **What stays `PROCESS ONLY` is enumerated rather than implied** (`DEC-0041` D11): result completeness, mutation accounting, write-scope adherence, `AUTHOR ≠ REVIEWER`, `ZERO MUTATION` basis, the `DEC-0035` §7 header rule, and citation drift.
- **One workflow now runs on a governance PR — and it cannot block one.** `.github/workflows/governance-checks.yml` is the **first** workflow here to trigger on a docs/governance pull request: of the eleven on `main`, only `data-engine-tests.yml` and `sg8-integration-local.yml` carry a `pull_request` trigger and **both are path-filtered to `services/data-engine/**`**. Its status is **`AUTOMATIC DETECTIVE · NOT MECHANICALLY MERGE-BLOCKING`**, `permissions: contents: read`, zero secrets, zero Environments, zero database or cloud, actions SHA-pinned, and it re-arms nothing. **`Protect main` (id `19697151`) carries exactly `deletion`, `non_fast_forward` and `pull_request` — there is no `required_status_checks` rule, so no CI check can block a merge on this repository at all**, and `C5` was forbidden to change that (`DEC-0041` D5, §9).

**State:**
- **Zero registered NOXUND agents are REAL PRODUCT EXECUTORS.** `governance_integrity_agent` is **PROPOSED-NOT-OPERATIONAL / RUNTIME-NOT-WIRED**; it binds nothing, and `DEC-0037` neither registers nor wires it.
- **Task-scoped roles are not agents**: instantiating one creates no registration, no boundary and no standing permission (`DEC-0037` D3). The registry-first provisioning gate in [`docs/agents/README.md`](../agents/README.md) is undisturbed.
- **Process independence is not mechanical independence — now measured, not inherited.** `C3` re-derived the live configuration and **promoted `DEC-0037` D12's two `REPORTED` claims to `VERIFIED`** (`DEC-0039` §3). No `CODEOWNERS` file exists; `required_approving_review_count` is `0` and `required_reviewers` is empty, so **independent approval is not mechanically required** and the same GitHub principal may author and merge — which PRs #86–#89 show is not merely possible but what happened, with zero GitHub reviews recorded (**F4a**, `ACCEPTED CURRENT LIMITATION`). A single admin may also prospectively alter rulesets (**F4b**, `ACCEPTED CURRENT LIMITATION` + `DETECTION REQUIRED LATER`) — **and `C5` has now adjudicated that detection as not buildable here**: it would need an admin-scoped secret `C5` may not create and, decisively, **its baseline would be an ordinary repository file owned by the same single admin it exists to detect**, so it is re-routed **out-of-band** to Phase F. **F4b's disposition is unchanged and is not weakened** (`DEC-0039` D11; `DEC-0041` D9). **Neither fact means current protections are ineffective, and `CURRENT ENFORCEMENT` is a separate question from `CONFIGURATION DURABILITY`:** three rulesets are active, **direct push to `main` is mechanically blocked**, `main` cannot be force-pushed or deleted, and the eight preserved refs are protected against update, force-push and deletion **with no bypass actor at all**. Mechanical enforcement was Phase-C unit `C5`, now landed — and it added **no** ruleset, required status check, branch protection, reviewer threshold or `CODEOWNERS` file, so **every sentence above about the current protection surface is unchanged by it** (`DEC-0041` D10, §15).
- **Two technical principals exist, and only one is mechanically least-privileged.** The Product Lead, the Product Orchestrator and every task-scoped role act as the **same** GitHub principal — the sole collaborator, holding repository `admin`. The **CI identity** is separate and partly constrained: **`can_approve_pull_request_reviews: false`** is a repository setting no workflow can override, so that principal **cannot approve a PR**; its contents restriction (repository-default `read`, `contents: read` in all eleven workflow files **on `main`**) is a **default rather than a ceiling** and holds for the workflows that exist, not for any conceivable one. Whether any GitHub App or bot has access is **UNKNOWN** — the enumeration is unavailable to the current credential, and that is **not** evidence that none exists (`DEC-0039` §3.3).
- **`packages/orchestrator` is decided: REDUCE** ([`DEC-0038`](decisions/DEC-0038-packages-orchestrator-disposition-reduce-to-approval-integrity-primitives.md) D1). It is **not** the Agent Governance V2 runtime foundation. The orchestration control plane is **NON-AUTHORITATIVE legacy**, and a named subset — command identity, approval binding/verification, single-use consumption-ledger semantics, safety classification — is retained as **candidate primitives, not adopted**. The topology remains **runtime-agnostic** and is confirmed, not disturbed, by that choice (`DEC-0037` §9).
- **State, so the decision is not misread:** the package remains on `main` **unchanged** — `DEC-0038` deletes, moves and edits nothing (D3) — and it is still imported by no file outside itself and run by no workflow. The command-identity, approval-binding and consumption-ledger primitives exist **only** at the preserved commit `151fb46bd5f335d26a93fc85d4dc9209340c9294` and are **not landed**.
- The runtime's approval gate is **UNBOUND · REPLAYABLE · UNAUTHENTICATED · NON-EXPIRING**, reproduced by execution at this base. It is **LATENT — UNREACHABLE**, because nothing drives the runtime *and* no handler can perform a destructive act. **That is reachability at this base, not a mitigation**, and it must be resolved before any wiring (`DEC-0038` §6).
- **Authenticated approver provenance is OPEN and routed to `C3`.** A content-addressed approval is not authenticated merely because it is well-bound: identity answers *what*, consumption answers *how many times*, neither answers *who* (`DEC-0038` §13).
- **That routed question is now answered — and the answer is that provenance is *not* currently provable.** [`DEC-0039`](decisions/DEC-0039-capability-technical-principal-independence-model.md) D6 adjudicates it: under one principal a runtime could not distinguish an authentic Product-Lead approval from an agent-produced one — `TECHNICAL APPROVER-PROVENANCE = NOT CURRENTLY SEPARABLE FROM SINGLE PRINCIPAL`. `DEC-0039` names the *category* of trust anchor that would be required, **implements none**, and adopts **P3 — HYBRID** (D9). **`IDENTITY ≠ CONSUMPTION ≠ PROVENANCE`, and hashing is not attestation: no primitive retained by `DEC-0038` D2 supplies approver provenance** (`DEC-0038` §13; `DEC-0039` §7).
- **The `AgentResult` envelope is decided: `NON-AUTHORITATIVE LEGACY`** (`DEC-0040` D16). `packages/orchestrator/src/core/result-schema.ts` remains on `main` **unchanged** — `DEC-0040` writes no code — and is a **partial transport projection** of a role result: it can carry a task id, an execution state, a summary, artifacts, errors and a next recommendation, and it **cannot express** a unit disposition, an artifact verdict, an evidence class, a finding lifecycle, mutation accounting, review functions held, or the role / instance / principal separation. Its required `agent: string` field asserts a **registered agent identity**, which `DEC-0037` D2/D3 forbid a task-scoped role from claiming — so a task-scoped Author cannot return a valid `AgentResult` at all. **It is not the governance result contract, and no unit satisfies `DEC-0040` by returning it.** The governance contract is prose, runtime-agnostic, and depends on no wired runtime.
- **`OrchestratorDecision`, `ROLE RESULT` and `GOVERNED UNIT CLOSEOUT` are three different artifacts** with three different producers and three different readers (`DEC-0040` D17). The `OrchestratorDecision` format contract, its four decision types and `TaskCommand` are **preserved intact**; a routing decision is never a closeout.
- **Verified drift, classified and not treated as a defect.** `C2`'s two-line reconciliation note shifted every subsequent line of `product-orchestrator-agent.md` by `+2`, so the line-number citations in `DEC-0035` §9, `DEC-0036` and `DEC-0037` no longer resolve to the clauses they name — `DEC-0037`'s `:598`/`:599` and `:565` now land on **different clauses** (`DEC-0040` §7). All three records are `FROZEN` and **none is edited**. **Most citations are rescued by quotation** — the quotation and the named clause govern and the number is a stale locator (`DEC-0040` D15). **Four are not, and are escalated rather than closed:** `DEC-0036` D2's `:128`, `:384` and `:790` and `DEC-0037`'s evidence-base `:565` are bare numbers with no quotation and no named anchor, classified **`UNRESOLVED — LOCATOR ONLY`**. **`C4` held no authorization to repair them** — a limit on that unit, not on `DEC-0040` D15, whose repair-at-the-citing-end **had an available performer**: both citing records are FROZEN but **neither is byte-frozen** (`DEC-0035` §3.3 reserves that grade to `DEC-0028` and `DEC-0033`), so each may receive a clearly-marked additive forward-status note from **an authorized unit**.
- **They are now repaired, at the citing end, by `C5` Limb A.** [`DEC-0041`](decisions/DEC-0041-mechanical-governance-enforcement.md) §7 appended one additive forward-status note to `DEC-0036` and one to `DEC-0037`, each after the existing closing `Related` line so nothing already there shifts. Every intended proposition was **re-derived directly from `docs/agents/product-orchestrator-agent.md`** and is now identified by **stable path + stable named section + verbatim quotation**: `:128` → §*Source of Truth*; `:384` → §*Agent Interaction Model*; `:790` → §*Output Format* › §*Decision sequencing*; `:565` → §*Task Decomposition Contract* › §*Regras de atribuição*. **`UNRESOLVED — LOCATOR ONLY` is discharged for those references.** **Re-derivation also corrected the extent of the finding:** the `DEC-0037` header carries **three** bare locators in one enumeration — all three pointing into §*Regras de atribuição* — not the single `:565` the routing named, and all three are repaired — **`DEC-0040` is not edited and no adjudication is revised**. **No historical text was rewritten, no old line number replaced in place, and the cited file was never edited to make an old number true again**; both records remain **FROZEN** and **ACTIVE / CURRENT**, and the old numbers stand as `HISTORICAL / STALE LOCATOR`. **Neither note is exempted from the new check** — both pass it on their merits, and the implementation has no baseline-exception mechanism at all. `C4` introduced **no new drift**: every edit it made at or above `:686` is line-count neutral, the file goes `859 → 881` with all `+22` lines below `:686`, and the four `DEC-0039` anchors plus `DEC-0038` §14's `:726` are unmoved.
- The Phase-A/B docs-only topology was a **temporary exception** (`DEC-0035` §14). `DEC-0037` establishes the permanent topology **prospectively** and does **not** retroactively convert Phase-A/B practice into standing authority.

---

## F. Authority model — bootstrap minimum

Binding authority is determined under `DEC-0035`. The minimum to act correctly:

- **Authority class, lifecycle and mutability are three independent axes.** `SUPERSEDED` is a lifecycle value, never an authority class.
- **Chronology does not silently supersede.** Date, number and commit order are irrelevant alone; **explicit supersession is required**.
- Two conflicting **CURRENT INTERNAL-NORMATIVE** authorities with no explicit edge → **`HOLD — AUTHORITY-CONFLICT`**: state both readings and escalate. The winner is never inferred.
- **Memory is NON-AUTHORITATIVE.** On conflict, the repository wins.
- **Current-state reporting never overrides normative authority.** On disagreement `DEC-0035` §4 gives two outcomes: an accurate fact is a **STATE-AUTHORITY DIVERGENCE** — escalated, never edited away; a wrong or outdated statement here is a **STALE-DESCRIPTIVE DEFECT**, repaired in this document.

Adjudicate from `DEC-0035`, not this summary. **At the verification base, no state-authority divergence was found.**

---

## G. Improvement Phase B — Canonical Context V2 unit state

These `B*` labels are **Phase-B units**, **not** the `B1`/`B2`/`B3` Phase-A *blocker* labels of `PHASE-A-CLOSEOUT-R1` §1.

- **B0** foundation assessment — an **unlanded in-session assessment** accepted by the Product Lead; **no file exists on `main`** and none should be searched for (`DEC-0035` header)
- **B1** authority / lifecycle / supersession model — **LANDED** (`DEC-0035`)
- **B2** compact current state — **this artifact**
- **B3** context index — **LANDED** ([`context-map.md`](context-map.md))
- **B4** task context pack — **LANDED** ([`task-context-pack.md`](task-context-pack.md))
- **B5** bounded context reconciliation — **LANDED** (PR #85, merge `262c71907f24375196d13dc6901f64aa354acfad`); it reconciled §B, §G, §H and §I here and the stale routing claims in `context-map.md`
- **B6** Phase-B closeout — **COMPLETE**; its output is [`PHASE-B-CLOSEOUT-R1`](../result/PHASE-B-CLOSEOUT-R1.md) plus this revision's terminal-state maintenance

**Phase B is CLOSED.** Closing it **creates no authority and starts nothing** — see §B *Authority*.

Unlanded identifiers (§B): `DEC-0035` names B0, B1, B3 and B5; **B2, B4 and B6 are defined by no canonical record on `main`** — `task-context-pack.md` is the artifact B4 produced and `PHASE-B-CLOSEOUT-R1` the artifact B6 produced, neither being a record defining its unit — and B2, B4, B5 and B6 each stood on their own authorizing GO. Phase B is no more complete than this.

**Implemented by B3.** The two-axis separation this document required of the index — **improvement-program routing** kept distinct from **product/engineering-trajectory routing**, so the index does not treat A–F as NOXUND's only historical axis — is carried by [`context-map.md`](context-map.md) §2, which assigns every ladder it registers to Axis 1 or Axis 2.

**B5 closed complete, and the one question it left open is now closed by decision — not by B5.** The authority class of `docs/product/scope-guardrails.md` stood at `HOLD — PRODUCT-LEAD ADJUDICATION REQUIRED`, which no DESCRIPTIVE-CURRENT surface may resolve. The Product Lead adjudicated it, and [`DEC-0036`](decisions/DEC-0036-scope-guardrails-authority-class-descriptive-current.md) landed the result: **`DESCRIPTIVE-CURRENT · CURRENT · EDITABLE`, carrying no INTERNAL-NORMATIVE authority of its own** — route to the governing source, not to the compilation (`context-map.md` §1). That record **discharges the `DEC-0035` §9 deferral by satisfaction; it supersedes nothing and edits no DEC.** It is a decision unit of its own, **not** a retroactive part of B5 and **not** a Phase-B unit label.

*Verified for this paragraph only against `262c71907f24375196d13dc6901f64aa354acfad`, 2026-08-20 (`DEC-0036` unit) — the descendant merge of this document's declared base. No other claim in this document was reverified at that base.*

---

## H. Open and deferred headlines

Headlines only, not an inventory. **Successor PostgreSQL architecture undecided**, **provisioning unexecuted**. **Agent-governance / runtime redesign is improvement Phase C.** **Historical context normalization is open with no successor venue** — B5 was a *bounded* reconciliation, not a corpus-wide normalization, and Phase B closed without it (`DEC-0035` Annex A rows 1–11 stay routed and not solved). It is **not** carried by any open phase; assigning it one is a Product-Lead decision. **The product / engineering trajectory is paused and incomplete** (§B). The `OD-*` namespace and its cross-series collisions are **not** inventoried here — B3 inventoried the six families in [`context-map.md`](context-map.md) §2; the open `OD-*` items themselves stay open and close only by Product-Lead decision.

**What B5 pruned, so pruned is not mistaken for overlooked.** `DEC-0035` Annex A rows 1–11 and its §11 step 4 routing of `docs/agents/orchestration-runtime.md`'s stale `Status: implementado` line were **considered and deliberately left out** of this bounded unit as historical normalization: Annex A is expressly *"NOT normative"* and *"creates NO obligation to edit any file listed below"*, and permits B5 to *"add to or prune this list"*. They remain **routed and not solved** — the `orchestration-runtime.md` warning is live in `task-context-pack.md` §5 case D.

---

## I. Routing

- Authority model, precedence, supersession → [`DEC-0035`](decisions/DEC-0035-canonical-context-v2-authority-lifecycle-supersession-model.md)
- Phase-A terminal state → [`PHASE-A-CLOSEOUT-R1`](../result/PHASE-A-CLOSEOUT-R1.md)
- Phase-B terminal state; Phase-B routed residuals → [`PHASE-B-CLOSEOUT-R1`](../result/PHASE-B-CLOSEOUT-R1.md)
- Decision records → `docs/product/decisions/**`
- Agent governance and contracts → `docs/agents/**`
- Locked product scope → [`context/01_MVP_Scope_PRD.md`](../../context/01_MVP_Scope_PRD.md)
- Topic-level routing; identifier disambiguation and namespace collisions; explicit relations; preserved evidence → [`context-map.md`](context-map.md)
- Assembling minimum sufficient context for one bounded task → [`task-context-pack.md`](task-context-pack.md) — a format aid; it grants no execution authority

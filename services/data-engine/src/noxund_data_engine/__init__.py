"""NOXUND Data Engine (scaffold).

Future responsibilities (NOT implemented in this foundation phase):
  - collection via the YouTube Data API;
  - immutable raw snapshots;
  - deterministic computation (Score, Velocity, Signals, Competition);
  - computed metrics + report snapshots;
  - audit trail and reproducibility.

No real collection, no scoring, no IA-generated numbers exist here yet.
See ../README.md and docs/agents/data-ai-pipeline-agent.md.
"""

__version__ = "0.0.0"

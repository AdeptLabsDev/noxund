[core]
	engine = pg
	target = spike
	# plan_file = sqitch.plan
	# top_dir = .
# [engine "pg"]
	# target = db:pg:
	# registry = sqitch
	# client = psql
[core "pg"]
	client = /tmp/work/psql-wrap.sh
[target "spike"]
	uri = db:pg://noxund_migrator@noxund-p3a-pg/sqitch_spike
	client = /tmp/work/psql-wrap.sh
